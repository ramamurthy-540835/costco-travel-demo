# Project State

## Project Reference

See: .coder/PROJECT.md (updated 2026-08-27)

**Core value:** Members get Costco-style negotiated rental rates and perks that stay honored and enforceable across booking, changes, cancellation, and billing — even though the platform never fulfills the rental itself.
**Current focus:** Project initialized — ready for planning

### Ad-hoc bugfix + UX session (2026-09-11 to 2026-09-14): TD-9/TD-10 closed, chat UI polish

Not tied to a formal PLAN.md — user-driven bugfix/polish pass on the Phase 9 Customer Assistant chat UI ahead of Phase 10 planning. Fixed and live-verified: **TD-9** (intermittent "no vehicles available" that succeeded on retry — `lib/graph/client.ts`'s fire-and-forget `pool.on('connect', ...)` AGE session setup raced each fresh connection's first query; replaced with an explicit `withGraphSession` checkout-await-release helper, verified with a 15-way concurrent smoke test) and **TD-10** (wrong vehicle proposed — a Toyota Corolla booked when the user asked for a Mercedes-Benz shown in results — fixed via an opaque `resultRef` composite reference token replacing separate `inventoryId`/`vendorId` fields, plus a `vehicleModel` defense-in-depth check in `propose_booking`; `chat-loop.ts` system prompt updated to match; verified live end-to-end via Playwright — model correctly asked for vendor disambiguation rather than guessing, and the confirmed proposal matched the exact vendor/model requested). Both added to ROADMAP.md's Technical Debt table as Fixed (TD-9, TD-10).

Also shipped, not tracked as separate tech debt (pure UX, no correctness bug): chat input auto-focus after each assistant reply; animated three-dot "thinking" indicator replacing a static "…" placeholder; reverted an earlier card-before-text experiment back to text-above-cards (user's own call, citing system-prompt phrasing like "take a look below" only reading correctly in that order — confirmed as the ChatGPT/Claude-convention default); and a text-rendering "beautify" pass (`AssistantText` now promotes single `\n` to paragraph breaks, adds `space-y-2 leading-relaxed` + list spacing, both chat bubbles capped at `max-w-[85%]`/`max-w-[90%]`) — visually confirmed via a diagnostic Playwright screenshot showing proper paragraph wrapping instead of one cramped block.

All diagnostic scripts/specs (`zzz-*`) and the temporary `auth.setup.ts` `networkidle`→`waitForTimeout` workaround (used repeatedly to route around a pre-existing, still-unfixed test-infra flake — real root cause not chased, out of scope) were created, used, and cleaned up/reverted each time — none left on disk. `npx tsc --noEmit` clean throughout. Files touched: `lib/graph/client.ts`, `agent-service/customer-assistant/tools.ts`, `agent-service/customer-assistant/chat-loop.ts`, `components/assistant-chat.tsx`, `app/api/bookings/[id]/cancel/route.ts` (dry-run cancellation quote now also returns `bookingId`, supporting the in-progress `CancellationSummaryCard` work tracked by the `velvety-leaping-dragon` plan file). Not yet committed as of this note — commit pending.

## Current Position

Milestone: v0.1 Ontology & Discovery/Checkout Core
Phase: 9 of 12 (Customer/Driver Assistant Agent) — reopened 2026-09-15 for 09-08 (get_booking_status sort fix + filter coverage). Otherwise fully complete (5/5 plans prior — 09-07 reopened+reclosed 2026-09-15; before that 4/4, reopened for 09-03 then 09-06, reclosed each time) — last transitioned to Phase 10 2026-09-10.
Plan: 09-08-PLAN.md complete — fixed `get_booking_status`/`GET /api/bookings` "latest bookings" sort (createdAt desc, not pickup-date desc) and added server-side `bookingId`/`pickupDate`/`city` filters plus a hardened server-side `vendorId` filter, replacing the client-side vendorId workaround in `agent-service/customer-assistant/tools.ts`. 09-08-SUMMARY.md created. Prior: 09-01 through 09-07 all complete (see phase directory SUMMARYs). Loop position (09-08): PLAN ✓ → APPLY ✓ → UNIFY ✓.

### 09-08 applied+unified (2026-09-15): get_booking_status sort fix + real filter coverage
Fixed the actual reported bug: "latest N bookings" was sorted by rental pickup date (`from`), not booking-creation date — now `.sort({ createdAt: -1 })` in `app/api/bookings/route.ts`. Extended `buildOwnBookingsFilter`/GET handler with real server-side filters: `bookingId` (merged into the same member-scoped filter object, never a bare `findById` — closes an IDOR risk flagged by adversarial review), `pickupDate` (single-day filter that fully replaces, not merges with, `from`/`to`), and `city` (resolved via a new `getInventoryIdsByCity()` graph query, `Inventory -[:LOCATED_AT]-> Location{city}`). Rewrote `get_booking_status` in `agent-service/customer-assistant/tools.ts` to pass all filters as real query params instead of the old self-flagged client-side vendorId-only workaround (which fetched the member's full history every time); kept the "return full list + note" fallback for vendorId-only near-misses, but bookingId/city/pickupDate misses now correctly return an empty list, never the unfiltered history. Added an automated Playwright regression test (`tests/e2e/regression/my-bookings.spec.ts`) asserting the bookingId ownership/miss behavior. Ran a 2-agent capped adversarial plan review before APPLY (1 blocker + 4 should-fix items, all folded into the plan and implemented — see 09-08-SUMMARY.md's Issues Encountered). Mid-APPLY, user asked whether `city` should also cover drop-off location — verified via full-codebase grep that no `dropoffCity` is ever persisted on `Booking` or filterable in the graph (it's a `get_quote`-only display detail); no code change needed, added a clarifying schema comment. `npx tsc --noEmit` clean throughout; `buildOwnBookingsFilter` logic verified via a temporary standalone script (all filter combinations, deleted after use) since the full Playwright regression run is currently blocked by a pre-existing, unrelated `playwright.config.ts` `baseURL` vs. `/agentic-travels` basePath mismatch (from 09-07) — flagged as a follow-up in 09-08-SUMMARY.md's Next Phase Readiness, not fixed in this plan. All 6 ACs Pass. Phase 9 reclosed (5/5 plans, `09-08-SUMMARY.md` created).

### 09-06 applied+unified (2026-09-10): A2A vendor-availability gate in the conversational booking flow

User's decision after the case-mismatch bugfix: the conversational assistant must make a real Vendor Agent (A2A) call before booking — the traditional/non-chat `/checkout` path is explicitly allowed to keep bypassing it. Implemented by inserting a `callVendorSkill('check_availability', ...)` gate into `propose_booking` (`agent-service/customer-assistant/tools.ts`), between the canonical inventory-row match and PaymentIntent creation — reused the existing (previously-unused-by-booking) A2A client and vendor-agent skill from Phase 8 with zero server-side/skill changes. Added one line to `chat-loop.ts`'s system prompt so a vendor-declined-availability error is surfaced as plain text (not retried like an id-mismatch). Live verification (real graph/Mongo/Stripe/vendor-agent, no mocks) found and fixed one real bug: `callVendorSkill`'s bare `fetch()` threw an uncaught rejection on `ECONNREFUSED` instead of returning the `VendorTaskError` its type promised — latent since the only prior caller (`get_quote`) was unused by booking; fixed with a try/catch in `vendor-agent-client.ts`. Verified live: successful proposal for open dates (real A2A round-trip), blocked proposal against a real conflicting Mongo booking (no PaymentIntent created), and clean failure message with the vendor-agent process stopped (restarted afterward). `app/api/bookings/route.ts` and `app/(checkout)/checkout/checkout-form.tsx` confirmed untouched. `npx tsc --noEmit` clean. All 4 ACs Pass. Phase 9 reclosed (4/4 plans, `09-06-SUMMARY.md` created).

### Manual audit + fixes (2026-09-10): Customer Assistant discovery→booking→modification→cancellation flow

User reported no car rentals appearing for Las Vegas; asked for a full flow audit (discovery→booking→modification→cancellation) via Playwright. Found and fixed three real bugs (TD-1/2/3 in ROADMAP.md's new Technical Debt section): (1) an unnecessary 5th slot-filling question blocking `search_inventory` even once all required slots were known — prompt-hardened in `chat-loop.ts`; (2) a near-miss addonId (e.g. `"gps"` vs. real `"gps_navigation"`) silently dropped from the booking/addon total with zero error — fixed in `tools.ts`'s `computeBookingTotal`/`propose_booking` and `app/api/bookings/[id]/addons/route.ts`; (3) a stale `.proposal` left on an earlier assistant message never cleared when a later `propose_booking`/`propose_addons` call produced a new one, causing two live `BookingSummaryCard`+Stripe payment forms to mount at once — fixed in `assistant-chat.tsx`'s `tool_result` handler.

Re-running `assistant-booking.spec.ts` after fix (3) surfaced a deeper, not-yet-fixed issue (TD-4): `propose_booking` appears to fire more than once per booking flow in some runs, and the real `POST /api/bookings` now intermittently 400s (PaymentIntent/amount mismatch) — very likely the actual mechanism behind the user's original Stripe "negotiated rate" screenshot. **Deliberately not chased further blind** — user decided (2026-09-10) to do this instrumentation as part of Phase 10's Phoenix tracing rather than continuing prompt-only guesswork. Full tracked list (TD-1 through TD-8, including the still-untested modification/cancellation flows and a missing cancellation regression spec) is in ROADMAP.md's new "Technical Debt" section, to be revisited once Phase 10 ships tracing. `scratch-audit.ts` (temporary diagnostic script) deleted.

### 09-01 applied (2026-09-08): Customer Assistant backend — all 4 tasks complete, all 6 ACs live-verified
All 4 tasks executed. Two real bugs found and fixed during Task 3's live E2E verification (not caught by Task 2's direct-handler tests): (1) `computeBookingTotal` was unconditionally charging all non-waived priced add-ons, causing `create_booking`'s downstream `POST /api/bookings` call to reject with a rate mismatch, since this tool chain has no `addonIds` param and the route defaults to `[]` — fixed by removing add-on computation, `totalPrice: dailyRate * nights` only. (2) `consumePendingProposal`'s original filter (`'pendingProposal.args': args`) required whole-embedded-document equality against Mongo — brittle against a model naturally restating/adding/reformatting incidental fields at confirm time (e.g. echoing a `from` it inferred from context, or reformatting a date string), and a subset-object match (the first attempted fix) can never satisfy whole-document equality either. Fixed by changing `consumePendingProposal` to match on caller-supplied identity fields via per-field dot-paths (`pendingProposal.args.<key>`) rather than the whole object; `create_booking`/`modify_booking`/`cancel_booking` now match on an identity key alone (`paymentIntentId`, or `bookingId`) and execute using the **originally-proposed args stored on the session**, not the model's restated ones — the mutation always reflects exactly what was quoted. Also fixed during Task 3: switched from a hand-rolled `OpenAI` client with a v1-GA `baseURL` (rejected by this Azure resource — `400 API version not supported`, including with a `defaultQuery` fallback) to the `openai` package's dedicated `AzureOpenAI` class, which manages the deployment-scoped URL + `api-version` param internally; capped `search_inventory`'s tool result to 10 rows (the full 1000+-row catalog blew past Azure's rate limit); stopped persisting `tool`-role messages into cross-turn session history (a bare `tool` message is only valid immediately following its own `tool_calls` message in the *same* request — replaying it standalone into a later turn was rejected by the API). All 6 ACs live-verified against real Mongo/graph/Vendor-Agent/Stripe/Clerk data through the actual `/chat` SSE endpoint (not direct handler calls, not mocks): AC-1 (standalone boot), AC-2 (real discovery data), AC-3 (server-side reject-without-propose, confirmed live — model self-corrects by re-proposing), AC-4 (Clerk cookie forwarded, never a model-supplied member id), AC-5 (real A2A JSON-RPC calls), AC-6 (payment gate — `create_booking` rejected pre-payment with the route's own "Payment has not succeeded" message, succeeds post-payment). Noted and documented as an accepted tradeoff (not a bug): a failed confirm attempt still atomically consumes that pending proposal, so the model must re-propose after a failed confirm (it does so automatically). `npx tsc --noEmit` clean throughout. README with real, reproducible curl/SSE examples and VOCABULARY.md's tool-vocabulary note written per Task 4. Temporary verification scripts (`verify-task2.ts`, `get-cookie.ts`, `setup-modify-test.ts`, `confirm-pi.ts`, `debug-session.ts`) all deleted per project convention — no permanent test scaffolding left behind. All 6 ACs Pass. Next: create `09-01-SUMMARY.md` and run `/coder:unify`, then either apply `09-02-PLAN.md` (chat UI, depends on 09-01) or continue elsewhere per user direction.

### 09-01/09-02 planned (2026-09-08): Customer Assistant backend + conversational chat UI
Classified as **complex** scope (multi-subsystem: LLM/Azure OpenAI integration, new Mongo session-state model, new SSE streaming endpoint, 7-tool orchestration layer, A2A client call to the Phase 8 Vendor Agent, plus a fully new conversational chat UI) — user confirmed splitting into 2 vertical-slice plans rather than one combined plan, mirroring the project's own 04-13/04-14 API+UI precedent. Two additional architectural decisions resolved via `AskUserQuestion` before planning: (1) auth boundary — the customer-assistant agent (a separate Node process, like the Vendor Agent) forwards the caller's **real Clerk session token** on every internal call to this app's existing `/api/bookings*` routes, rather than importing `policy.ts`/models directly — reuses every existing server-side integrity check (rate recompute, dry-run/confirm, optimistic-lock CAS) with zero duplicated business logic; (2) LLM client — `openai` npm package pointed at Azure's v1 GA `baseURL` (`${AZURE_OPENAI_ENDPOINT}/openai/v1/`), matching `mastech-agentic-commerce`'s `chat.py` pattern and reusing this repo's existing `AZURE_OPENAI_*` env vars (already used by `graph/scripts/backfill_synonyms.py`). User also explicitly required the customer-assistant agent be its own independently-deployable service (own process/port, no Next.js coupling) — already the plan, mirroring Phase 8's `agent-service/vendor-agent/` convention exactly; confirmed Agent Lab (Phase 10) is a separate deployable service too, out of scope for Phase 9. `09-01-PLAN.md` (wave 1, no deps, `autonomous: true`, 3 tasks): scaffolds `agent-service/customer-assistant/` (env loader, Mongo `ConversationSession` store, npm script), builds the 7-tool chain (`search_inventory`, `get_quote`, `propose_booking`/`propose_modification`/`propose_cancellation`, `create_booking`/`modify_booking`/`cancel_booking`, `get_booking_status`, `get_cancellation_policy`) with a server-side propose→confirm gate enforced by session state (never by prompting), a Vendor Agent A2A JSON-RPC client, and the Azure OpenAI streaming tool-calling loop behind `POST /chat` SSE; writes README + VOCABULARY.md tool-vocabulary note. `09-02-PLAN.md` (wave 2, `depends_on: ["09-01"]`, `autonomous: true`, 2 tasks): builds the chat launcher + panel (`components/assistant-launcher.tsx`/`assistant-chat.tsx`) consuming 09-01's `/chat` SSE contract, mirroring commerce's `SearchModal.tsx` pattern but using this app's existing Dialog primitives; explicit Confirm/Cancel UI gate on every mutating proposal (never auto-confirmed from model text alone); mounted globally via `app/layout.tsx`/`components/header.tsx`. Coherence check: no conflicts against PROJECT.md's Core Value (propose→confirm gate directly serves rate/perk integrity), prior Phase 8 Decisions (A2A surface consumed via JSON-RPC, not reimplemented), or ROADMAP.md's Phase 9 scope note. `enterprise_plan_audit` not present in `.coder/config.md` — standard reporting flow applies. Awaiting the standing capped adversarial plan-review before APPLY approval, per unbroken project precedent since 03-02.

### 08-01 applied and unified (2026-09-08): Vendor Agent A2A layer complete
All 3 tasks executed exactly as adversarially-reviewed, no deviations during APPLY. `agent-service/vendor-agent/` stood up (Node `http` module, no Express/Fastify): static Agent Card at `/.well-known/agent-card.json` (4 skills, live-verified), JSON-RPC `/a2a` implementing `message/send` with correct error responses (-32700 parse error, -32601 method not found), and `agent-service/vendor-agent/skills.ts` wiring all 4 skills (`check_availability`, `apply_modification`, `apply_cancellation`, `get_vendor_policy`) as thin wrappers over `lib/vendor-integration/policy.ts`/`lib/graph/queries.ts` via relative imports. All 4 skills live-verified against real seeded data (`Alamo`/`RC10001`) for both success and failure paths (unknown vendor/inventory, cutoff violation) — every failure path returns a clean `failed` Task, never a thrown exception/500. `npx tsc --noEmit` clean throughout. `agent-service/vendor-agent/README.md` written with real, reproducible curl examples (re-verified live, not aspirational). ONTOLOGY.md/VOCABULARY.md updated with the new A2A skill surface and skill-name vocabulary. All 4 ACs Pass. `08-01-SUMMARY.md` created. Phase 8 complete (1/1 plans, no further plans requested). Next: Phase 9 (Customer/Driver Assistant), which can call this Vendor Agent's `/a2a` surface directly and land at `agent-service/customer-assistant/`.

### 08-01 planned (2026-09-07): Vendor Agent server scaffold + A2A skill wiring
Two blocking decisions resolved via `AskUserQuestion` before planning could proceed (ROADMAP.md's explicit Phase 8 gates): A2A conformance depth → **minimal now** (`submitted|completed|failed` only), with the `TaskState` union and skill-dispatch shape built to include `input-required`/`auth-required` as forward-compatible unimplemented members so Phase 9 can extend without a rewrite; deployment topology → **one platform-hosted process, parameterized per vendor** (not one process per vendor). Both recorded in ROADMAP.md's Phase 8 Decisions, with the extensibility requirement also cross-referenced into Phase 9's Scope. Classified as a single standard-track plan (3 tasks) rather than a split — most of the underlying logic already exists in `lib/vendor-integration/policy.ts` (04-13's `quoteModification`/`checkAvailability`/`checkModificationCutoff`/`quoteCancellation`, `checkAvailability` already date-range/Mongo-overlap-capable, closing the "needs new design" gap ROADMAP.md's Phase 8 scope note had flagged) and `lib/graph/queries.ts`'s `getVendorPolicy()` — Phase 8 wires to these, doesn't design new logic. `08-01-PLAN.md` created (wave 1, no deps, `autonomous: true`): Task 1 builds `agent-service/vendor-agent/server.ts` (Node's built-in `http` module — no Express/Fastify dependency exists in this project — static Agent Card at `/.well-known/agent-card.json`, JSON-RPC 2.0 `/a2a` implementing `message/send`, the extensible `TaskState` union, new `vendor-agent:dev` npm script). Task 2 wires the 4 skills (`check_availability`, `apply_modification`, `apply_cancellation`, `get_vendor_policy`) as thin wrappers around `policy.ts`/`queries.ts`, each calling `lib/mongodb.ts`'s `connectToDatabase()` before any Mongo-backed skill (mirrors every existing API route's pattern — `policy.ts` itself never connects) and returning a `failed` Task (never a 500) on any thrown error or unresolvable input. Task 3 documents the surface in ONTOLOGY.md/VOCABULARY.md plus a live-verified `agent-service/vendor-agent/README.md`. Boundaries: `policy.ts`, the Phase 4 route call-sites, and Phase 11's docker-compose are explicitly untouched. Directory choice — a new top-level `agent-service/` folder (not repo-root `vendor-agent/`) — mirrors `mastech-agentic-commerce`'s `agent-service/` layout per user request, and leaves room for Phase 9's Customer/Driver Assistant to land at `agent-service/customer-assistant/` alongside it. Coherence check: no conflicts against PROJECT.md's Vendor-integration-boundary constraint, prior Decisions, or ROADMAP.md's Phase 8 scope — silent pass. `enterprise_plan_audit` not present/enabled in `.coder/config.md` — standard reporting flow applies. Awaiting the standing capped adversarial plan-review (max 3 agents, max 2 rounds) before APPLY approval, per unbroken project precedent since 03-02.

### 08-01-PLAN.md adversarially reviewed (2026-09-07): 1 full review + 1 targeted spot-check, 2 agents total
Round 1 (full review against real source — `policy.ts`, `queries.ts`, `mongodb.ts`, `client.ts`, `package.json`) found and fixed 3 issues, all edited directly into the plan: (1) execution-blocking — `lib/mongodb.ts`/`lib/graph/client.ts` read env vars lazily but only Next's own dev/build process auto-loads `.env.local`; a bare `tsx agent-service/vendor-agent/server.ts` run would have hard-crashed on the first DB call with no `dotenv` dependency anywhere in the repo to fall back on — fixed by adding an explicit dependency-free `fs`-based `.env.local` loader to Task 1's `server.ts`, run before any DB-touching import/call. (2) import-path ambiguity — Task 2's skill handlers didn't specify alias vs. relative imports for reaching `lib/` from outside Next's module graph; fixed to require relative imports (`../../lib/...`). (3) AC-3's gherkin wrongly described `apply_cancellation` as taking `vendorId`/`inventoryId`/date range — its real backing function (`quoteCancellation`) takes `vendorId`/`hoursUntilStart`; fixed. Round 2 (targeted spot-check of all 3 fixes, incl. exact relative-path depth-counting against real repo structure) confirmed all 3 PASS, no new inconsistencies introduced. Final verdict: safe to APPLY.

### 07-03 planned (2026-09-07): composable retriever tools
Ran `/coder:plan` for Phase 7 Plan 07-03 per user's explicit "1" selection (continue to 07-03) after 07-02's unify report. Read ROADMAP.md's Phase 7 scope note ("2-3 composable retriever tools ... per the GraphRAG multi-tool pattern") and `lib/graph/queries.ts`/`client.ts` to confirm the existing pattern is entirely bespoke-per-caller (no generic label/property-parameterized query exists yet). Classified as standard track (2 tasks, one architectural decision — a new generic module alongside the existing bespoke one, not a replacement). `07-03-PLAN.md` created (wave 1, no deps, `autonomous: true`): Task 1 (auto) builds `lib/graph/retrievers.ts` — `lookupNode(label, matchProperty, matchValue)`, `traverse(fromLabel, fromProperty, fromValue, edgeType, toLabel, direction)`, `resolveSynonym(term)` (cross-label search over every `synonyms`-bearing label: VehicleClass/Perk/AddOn/MembershipTier/Location), following `queries.ts`'s existing `VertexResult`/`.properties`-unwrap convention exactly, params always passed through `runCypher`'s `$`-parameterized args (never string-interpolated) to avoid Cypher injection — only label/edge-type names are interpolated, and those are fixed literals at call sites, never raw user input. Task 2 (auto) demonstrates all 3 tools with real live output in a new "Retriever Tools" section of `agent-lab`'s HTML demo (mirrored via equivalent raw Cypher in the Python generator script, since it can't import TypeScript) and documents the surface in ONTOLOGY.md/VOCABULARY.md as the sanctioned query path for Phase 8/9 agent code. Boundaries: existing bespoke `queries.ts` functions and all Phase 4-6 call-sites untouched — this plan adds a parallel generic surface, not a refactor. AC-2/AC-3 explicitly cross-check the new generic functions against 07-01's `getEquivalenceCandidates` and the existing `getNegotiatedTermsForVendor`, not just independent correctness. Coherence check: no conflicts against PROJECT.md/Decisions/ROADMAP.md's Phase 7 "Not in scope" note (no LLM/agent runtime code) — silent pass. Awaiting the standing capped adversarial plan-review (max 3 agents, max 2 rounds) before APPLY approval, per 07-01/07-02 precedent.

### 07-03-PLAN.md adversarially reviewed (2026-09-07): 1 full review + 1 targeted spot-check, 2 agents total
Followed the standing capped adversarial-review convention. Round 1 (full review against real source — `client.ts`, `queries.ts`, `schema.py`, `generate_knowledge_graph_demo.py`, `package.json`, `.env.local.example`) found 1 BLOCKING + 1 IMPORTANT issue, both fixed in place: (1) BLOCKING — AC-3/Objective's `resolveSynonym` label list (`VehicleClass, Perk, AddOn, MembershipTier, Location`) omitted `Intent`, which really carries a non-empty `synonyms` property per `verify_graph.py`'s own check and the `agent-lab` demo script's `Intent.synonyms` query (seeded via `data/synthetic/agent_intents.json`, Phase 2's 02-01) — fixed to the correct 6-label set. (2) IMPORTANT — the plan told the executor to verify the synonym-label list against `graph/graph_service/schema.py`, but that file only defines `NODE_LABELS`/`EDGE_ENDPOINTS` (label/edge names, no property-level schema) and structurally cannot answer the question — fixed to redirect to the real sources (`load_seed_data.py`'s hand-seeded `*_SYNONYMS` dicts, `verify_graph.py`'s checks, the demo script's Intent query). No issues found in Cypher syntax, injection-safety claims, AC-2's traverse examples against real `EDGE_ENDPOINTS`, Task 1's `tsx`/env-var verify steps, or Task 2's Python-mirroring approach for the agent-lab demo section. Round 2 (targeted spot-check of both fixes, including an exhaustive grep of all 14 `NODE_LABELS` against every script referencing `.synonyms`) confirmed both fixes PASS and confirmed the 6-label set is now complete — no 7th synonym-bearing label exists. Final verdict: safe to APPLY.

### 07-02 planned (2026-09-05): LLM-generated synonym backfill + Location vocabulary enrichment
Ran `/coder:plan` for Phase 7 Plan 07-02. Confirmed via direct inspection of `graph/scripts/load_seed_data.py` that all existing `*_SYNONYMS` dicts (VehicleClass/Perk/AddOn/MembershipTier) are hand-written, not LLM-generated despite ROADMAP.md's phrasing, and that `Location` nodes carry no `synonyms` property at all — the genuine, unambiguous gap this plan closes. Surfaced a real blocking ambiguity (no LLM SDK/API key exists anywhere in this project) via `AskUserQuestion`; **user chose OpenAI API** (over Anthropic API or a deterministic stub). `07-02-PLAN.md` created (wave 1, no deps, `autonomous: false`): Task 1 (auto) builds `graph/scripts/backfill_synonyms.py` — OpenAI forced-tool-call (`generate_synonyms`, 2-4 synonyms), `--labels`/`--force`/`--model` CLI, idempotent (skips nodes with existing non-empty `synonyms` unless `--force`), reuses `load_seed_data.py`'s connection pattern; adds `openai` to `graph/requirements.txt` and documents `OPENAI_API_KEY` in `.env.local.example` (no real key committed). Task 2 (checkpoint:human-action, blocking) — user must add a real `OPENAI_API_KEY` to `.env.local` and run the script live against `Location`, confirming all 10 cities get non-empty synonym lists and a second run is a no-op. Task 3 (auto) extends `verify_graph.py` with a `Location.synonyms` non-empty check and updates `VOCABULARY.md`/`ONTOLOGY.md`. Boundaries: existing hand-curated `*_SYNONYMS` dicts explicitly untouched — this plan only closes the Location gap, doesn't retroactively re-generate the others. Coherence check: no conflicts against PROJECT.md/Decisions/ROADMAP.md's Phase 7 scope — silent pass.

### 07-02-PLAN.md adversarially reviewed (2026-09-07): 1 full review + 1 targeted spot-check, 2 agents total
Followed the standing capped adversarial-review convention. Round 1 (full review against real source — `load_seed_data.py`, `verify_graph.py`, `connection.py`, `agtype.py`, `requirements.txt`, `.env.local.example`, `VOCABULARY.md`, `ONTOLOGY.md`) found 2 BLOCKING + 3 IMPORTANT/MINOR issues, all fixed in place: (1)/(2) BLOCKING — the plan's pseudocode for both `backfill_synonyms.py`'s `fetch_nodes`/`needs_backfill` and `verify_graph.py`'s new Location check assumed `parse_agtype()` returns a flat property dict; it actually returns the full vertex `{"id","label","properties":{...}}`, matching `verify_graph.py`'s own `resolve_one()` pattern (`.get("properties", {}).get(...)`) — as originally written, every node would read as empty, breaking AC-2 idempotency and permanently failing the new verify check. Fixed both task descriptions to require nested `properties` access. (3) IMPORTANT — dropped a stray "commit the connection before closing" instruction that contradicted `get_connection()`'s real autocommit setup (matches `load_seed_data.py`, which never calls `.commit()`). (4) IMPORTANT — added explicit `cur.fetchall()` step after `cypher()`, which only executes and doesn't fetch. (5) MINOR — fixed task numbering gap (checkpoint task had no name/number between Task 1 and Task 3) by naming it "Task 2." Round 2 (targeted spot-check of all 5 fixes) confirmed every fix VERIFIED-CORRECT against actual source. Final verdict: safe to APPLY.

### 07-02 APPLY: LLM provider switched to Azure OpenAI mid-checkpoint (2026-09-07)
Task 1 was built and verified (`ast.parse` + `--help`) against plain OpenAI per the reviewed plan. At the Task 2 human-action checkpoint, user reported they'd already provisioned Azure OpenAI credentials (`AZURE_OPENAI_API_KEY`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT_NAME`/`AZURE_API_VERSION`, added to `.env.local`) instead of plain OpenAI, and asked to switch. Classified as a Spec-level change (the plan's own AC-1/Task 1/Task 2 text named OpenAI specifically), so the plan text was updated first, then code: `backfill_synonyms.py` now builds an `openai.AzureOpenAI` client (`api_key`/`azure_endpoint`/`api_version` from env) instead of `openai.OpenAI`, `--model` defaults to `None` and falls back to `AZURE_OPENAI_DEPLOYMENT_NAME` (Azure's `model` param must be a deployment name, not a family name like `gpt-4o-mini`), and `main()`'s env-var preflight check now requires all 4 Azure vars. `.env.local.example` updated to list the 4 Azure vars (no real values) instead of `OPENAI_API_KEY`; `graph/requirements.txt`'s `openai>=1.0,<2.0` pin is unchanged (the `AzureOpenAI` client ships in the same package). Re-verified: `ast.parse` clean, `python3 graph/scripts/backfill_synonyms.py --help` runs without error. Checkpoint re-presented to user for a live run against the real Azure deployment.

### 07-02 Task 2 checkpoint verified live (2026-09-07): real Azure OpenAI backfill run
Sourced real Azure credentials from `.env.local` and ran `python3 graph/scripts/backfill_synonyms.py --labels Location` live. All 10 `Location` nodes updated with real, non-hardcoded synonyms via forced-tool-call Azure OpenAI responses (e.g. `Las Vegas: ['Vegas', 'Sin City', 'The Strip', 'City of Lights']`, `New York: ['NYC', 'The Big Apple', 'New York City', 'Gotham']`) — AC-1 satisfied. Re-ran with no flags: all 10 reported `skipped (already populated)`, 0 updated — AC-2 (idempotency) confirmed. Also resolved an open question from the Azure-provider switch: the sibling `mastech-agentic-commerce` project's `backfill_synonyms.py` uses a plain `OpenAI` client with `base_url` pointed at Azure's `v1`-preview endpoint plus an explicitly-constructed real `httpx.Client()` (no `api_version` needed) — inspected but not adopted here, because this project's installed `openai==3.2.0` vendors an internal `httpx2` fork whose `http_client` param strictly validates against `httpx2.Client`/`is_legacy_httpx_sync_client`, rejecting a real `httpx.Client()` instance outright. The dedicated `AzureOpenAI(api_key=..., azure_endpoint=..., api_version=...)` client plus the `SSL_CERT_FILE`/certifi workaround (already applied to fix a `truststore` SSL recursion bug in that same vendored fork) is the correct approach for this SDK version and was kept unchanged. Task 2 checkpoint approved; proceeding to Task 3.

### 07-02 Task 3 completed (2026-09-07): verify_graph.py check + documentation
Added a `Location.synonyms` non-empty check to `verify_graph.py` (mirrors the file's existing `PASS`/`FAIL` convention, nested `properties` access per the adversarial review's fix) — `PASS Location.synonyms non-empty (10/10)`. Full `verify_graph.py` run surfaced one pre-existing, out-of-scope failure: `Member: expected=100 actual=101` — data drift unrelated to this plan (not caused by any 07-02 change; the new Location check itself passes cleanly). Flagged to user, not fixed here (07-02's boundaries are Location-property-only). Updated `VOCABULARY.md` (new "Agent-support vocabulary (Phase 7)" section distinguishing LLM-generated `Location.synonyms` from the hand-curated Phase 2 table, notes the script is reusable via `--labels`) and `ONTOLOGY.md` (`Location` row now lists `synonyms`, Phase 7 gaps paragraph updated to mark 07-02 done). `grep -c backfill_synonyms` on both docs: non-zero (2 and 1 respectively). AC-3 satisfied. All 3 ACs now met — 07-02 ready to unify pending the pre-existing Member-count issue being separately triaged.

### 07-02 Task 4 added and applied (2026-09-07): self-contained HTML ontology/knowledge-graph viewer
User requested, mid-APPLY (before unify), a self-contained HTML viewer for Vocabulary/Taxonomy/Thesaurus/Ontology/Knowledge Graph — "similar to or better than" the one in `mastech-agentic-commerce/agent-lab`. Added as Task 4 to `07-02-PLAN.md` (files_modified extended), then built and ran immediately. New `agent-lab/` folder (project had none): `agent-lab/requirements.txt` (`psycopg[binary]`), `agent-lab/scripts/generate_knowledge_graph_demo.py` — read-only reporting script mirroring the sibling's pattern (single HTML string, no server/build step, vis-network via CDN for the graph view), adapted to this project's schema: imports `VEHICLE_CLASS_SYNONYMS`/`PERK_SYNONYMS`/`ADDON_SYNONYMS`/`MEMBERSHIP_TIER_SYNONYMS` directly from `graph/scripts/load_seed_data.py` (real code, not re-typed), live-queries `Location.synonyms` (this plan's own LLM-generated data), `VehicleClass PARENT_OF` taxonomy groupings, `MembershipTier` rank, `VocabularyTerm SYNONYM_OF` naming-variance and `Intent.synonyms` (graph-backed thesaurus), `Vendor.included_miles` (mileage normalization, documentation-only), and per-label/edge-type counts (mirrors `verify_graph.py`'s pattern). Knowledge Graph section renders the full vocabulary layer (10 small node types) plus a bounded sample (first 8 by id) of Member/Inventory/Reservation, avoiding an unreadable 1000+-node dump. Hit one bug during the live run: the shared `cypher()` helper's single-column `AS (result agtype)` output spec didn't match multi-column `RETURN` queries (`psycopg.errors.DatatypeMismatch`) — fixed by adding a `columns=N` parameter that builds the column-def list dynamically, then adding `columns=2`/`columns=3` at each multi-column call site. Re-ran successfully: `agent-lab/docs/knowledge_graph_demo.html` written (59,914 bytes), spot-checked for real data (`Las Vegas` → `Vegas`/`Sin City` chips present) and valid embedded graph JSON (101 nodes, 102 edges parse cleanly).

### 07-02 drift cleanup + knowledge-graph detail expansion (2026-09-07)
User: "Lets clear out the drift.. clean up if there is anything requied. Also can the knowledge graph shown be little more detailed showing all the parts of ontology rather than just the few transactions." Two fixes, both root-caused before acting:
1. **Member drift resolved**: root-caused the `Member: expected=100 actual=101` mismatch flagged in Task 3. Found one stray `Member` node (`member_id: "6a918a636e134d1cdc979023"`, not present in `members_100.json`, missing all real Member properties — no name/email/tier) with exactly one edge, `RESERVED`, to an `Inventory` node. Grepped the codebase: `RESERVED` is created only by `lib/graph/mutations.ts`'s `recordReservation()` (`MERGE (m:Member {member_id: $memberId}) ... CREATE (m)-[:RESERVED ...]->(i)`) — real live-app booking-mutation code, not a seed script. `MERGE` silently creates a bare Member node if a booking is ever recorded against a member_id not already in the graph, so this is leftover manual/dev test data from exercising the booking flow, not a seed-pipeline bug. Deleted the stray node + its edge (`DETACH DELETE`) after confirming its shape. Re-ran `verify_graph.py`: `ALL CHECKS PASSED`, zero drift.
2. **Knowledge-graph detail expanded**: found `NegotiatedTerm` (30 rows) was entirely absent from both `VOCAB_LABELS` and `SAMPLE_LABELS` in `agent-lab/scripts/generate_knowledge_graph_demo.py` — one full ontology node type was missing from the graph view, not just under-sampled. Fixed by moving `NegotiatedTerm` and `Member` (100 rows, small enough to show fully) into the full-dump label set, keeping only `Inventory`/`Reservation` (1000 rows each) sampled — raised their sample limit from 8 to 40. Regenerated `agent-lab/docs/knowledge_graph_demo.html`: now 287 nodes / 562 edges covering all 14 ontology node types (was 101 nodes / 102 edges, 3 sampled types). Verified via embedded `GRAPH_DATA` JSON label-count breakdown.

Files touched: `graph/scripts/verify_graph.py` (no code change — re-run only), `agent-lab/scripts/generate_knowledge_graph_demo.py`, `agent-lab/docs/knowledge_graph_demo.html` (regenerated). Live graph mutated: one stray `Member` node + its `RESERVED` edge deleted. 07-02 now fully clean, ready to unify.

### 07-03 unified (2026-09-07)
`07-03-SUMMARY.md` finalized — all 4 ACs Pass (AC-1: lookupNode verified live incl. null-miss case; AC-2: traverse cross-checked byte-for-byte against `getNegotiatedTermsForVendor`/`getEquivalenceCandidates`; AC-3: resolveSynonym case-insensitive + empty-array-for-missing-term, both live-verified; AC-4: agent-lab demo + both docs updated, grep-confirmed). Two auto-fixed issues documented (MembershipTier key-field mismatch; AGE Cypher's missing `ANY(...WHERE...)` list-predicate support, discovered via live execution not review). One frontmatter discrepancy flagged (`files_modified` listed `queries.ts`, which the plan's own Objective/Output said would stay unchanged and did). Loop closed. **This is the last plan in Phase 7 (3/3 `*-SUMMARY.md` now present)** — phase-completion transition triggered.

### 07-02 unified (2026-09-07)
`07-02-SUMMARY.md` finalized — all 3 ACs Pass (AC-1: real Azure OpenAI backfill, 10/10 cities; AC-2: idempotent, confirmed via second run; AC-3: both docs updated, grep-confirmed). One deviation from the reviewed plan (Spec-level: OpenAI→Azure OpenAI switch mid-checkpoint, documented with the SSL/httpx2 workaround), one scope addition folded into the same plan rather than spun into a new one (Task 4: self-contained HTML ontology/knowledge-graph viewer, `agent-lab/`), and one post-APPLY cleanup pass (Member-drift root-cause+fix, knowledge-graph detail expansion to all 14 ontology node types) — all captured in SUMMARY.md's dedicated sections. `verify_graph.py` reports `ALL CHECKS PASSED`, zero drift. Loop closed. Not the last plan in Phase 7 (07-03 composable retriever tools remains TBD, no `*-SUMMARY.md` count match yet) — no phase transition triggered. Phase 7 progress remains partial pending 07-03.

### 07-01 applied (2026-09-05): EquivalenceCluster/PART_OF_CLUSTER substitution relation
Executed via `/coder:apply` after explicit user approval ("Approved, run /coder:apply on 07-01"). All 3 auto tasks executed and qualified, no gaps/drift, no deviations — the adversarial review's fixes (see above) meant every command ran clean on the first attempt. Task 1: added `data/synthetic/equivalence_clusters.json` (5 clusters partitioning all 9 real VehicleClass names — Budget Sedan: Economy/Compact; Standard Sedan: Mid-size/Full-size; Family Utility: SUV/Minivan; Premium: Luxury/Convertible; Utility Truck: Pickup), extended `graph/scripts/load_seed_data.py` (partition-completeness assert + `EquivalenceCluster` node/`PART_OF_CLUSTER` edge seeding) and `graph/scripts/verify_graph.py` (count check + a new per-class edge-completeness check) — also extended `graph/graph_service/schema.py`'s `NODE_LABELS`/`EDGE_ENDPOINTS` (real dependency for `verify_graph.py`, not itself listed in the plan's `files_modified`, flagged here per this project's SUMMARY-accuracy convention). Ran `load_seed_data.py` then `verify_graph.py` — `ALL CHECKS PASSED`, including the new partition check (5 clusters, 9/9 real classes each with exactly one `PART_OF_CLUSTER` edge). Task 2: added `getEquivalenceCandidates(className)` to `lib/graph/queries.ts`, matching the file's existing `VertexResult`/`getVehicleClasses` mapping pattern exactly. Added `tsx` as a devDependency (`npm install -D tsx`) to run a temporary live-verification script (created, run, then deleted per the plan's own instruction) — confirmed live against the real running graph: `'Economy'` → `['Compact']`, `'Pickup'` → `[]` (singleton cluster, correctly no siblings). `npx tsc --noEmit` clean. Task 3: updated `ONTOLOGY.md` (new Entities row, new Relations line, new "Phase 7" gaps paragraph noting 07-02/07-03 as TBD follow-ons) and `VOCABULARY.md` (new cluster-listing table + rationale distinguishing this from `TAXONOMY.md`'s Standard/Utility/Premium split) — `grep -c EquivalenceCluster` on both files returned 3 each, non-zero. All 3 ACs (seeding invariant, query correctness, doc updates) passed, verified live not just by code trace.

### 07-01 unified (2026-09-05)
`07-01-SUMMARY.md` created — all 3 ACs Pass, no deviations from the plan itself (one dependency file, `graph/graph_service/schema.py`, touched beyond the plan's `files_modified` list, documented). Loop closed. Not the last plan in Phase 7 (07-02/07-03 remain TBD, no `*-SUMMARY.md` count match yet) — no phase transition triggered. Phase 7 progress remains partial pending 07-02/07-03.

### 07-01-PLAN.md adversarially reviewed (2026-09-05): 1 full review + 1 targeted spot-check, 2 agents total
Followed this project's standing capped adversarial-review convention (see Decisions below, from 03-02). Round 1 (full review against real source — `load_seed_data.py`, `client.ts`, `queries.ts`, `verify_graph.py`, `docker-compose.yml`, `package.json`, `scripts/*.mjs`) found 1 BLOCKING + 3 IMPORTANT issues, all fixed in place: (1) Task 2's `getEquivalenceCandidates` was written against `runCypher<VehicleClass>` returning flat properties — wrong, every vertex-returning query in `queries.ts` actually returns `{properties}` via a local `VertexResult` interface and maps `row.properties as unknown as T`; fixed to match `getVehicleClasses`'s exact pattern. (2) Task 1's `<verify>` used a raw `psql` cypher call that would fail without the `LOAD 'age'`/`search_path` setup every real connection does — redirected to extend the project's existing `graph/scripts/verify_graph.py` instead of inventing a new ad hoc check. (3) Task 2's `<verify>` assumed `npx tsx` — not installed, and no existing `.mjs` script imports a `.ts` file (confirmed in round 2 that this would hard-fail: no `"type": "module"`, extensionless `./client` specifier won't resolve). Fixed by explicitly adding `tsx` as a devDependency as part of Task 2 (added `package.json` to `files_modified`), verify script now `.ts` run via `npx tsx`. (4) Task 1 had no partition-completeness check for the 9 real VehicleClass names — added an explicit assert-before-seed instruction. Also added one boundary line (no wiring `getEquivalenceCandidates` into `findEquivalentInventory`/routes — that's Phase 8/9's concern). Round 2 (targeted spot-check of all 4 fixes) confirmed fixes 1/3/4 correct as written; caught that fix 2's first draft (plain `.mjs`) would still fail for the reason above — fixed immediately per above. Final state: plan is internally consistent and grounded in verified real source; ready for user approval.

### Roadmap Phase 7 planned (2026-09-05): split into 3 plans
Classified Phase 7 as complex scope (3 distinct concerns: EquivalenceCluster substitution relation, LLM-driven synonym backfill + location vocabulary, composable retriever tools — 6+ tasks, multiple subsystems). User chose (via AskUserQuestion) to split into vertical-slice plans rather than one large plan: **07-01** (EquivalenceCluster/PART_OF_CLUSTER substitution relation, new `data/synthetic/equivalence_clusters.json`, `getEquivalenceCandidates()` query fn, ONTOLOGY.md/VOCABULARY.md updates — wave 1, no deps, autonomous) created now. **07-02** (synonym backfill script + Location vocabulary) and **07-03** (composable retriever tools: lookup/traverse/synonym-resolve) remain TBD, to be planned after 07-01 is approved/applied — neither has a genuine dependency on 07-01's output, so they could apply in parallel with it once planned. No SPECIAL-FLOWS.md exists in this project — skills section omitted from 07-01-PLAN.md. Coherence check: no conflicts found against PROJECT.md constraints, prior decisions, or ROADMAP.md's Phase 7 scope — silent pass.

### 06-06 applied+unified (2026-09-04): smoke-suite gap fill — Phase 6 fully complete
Executed via `/coder:apply` on `06-06-PLAN.md` after explicit user approval ("Approved, run APPLY"). Added a new `test.describe('account routes', ...)` block to `tests/e2e/smoke/smoke.spec.ts` with one test asserting `/bookings` returns a response status below 500 and renders the "Sign in to see your bookings" gate for an unauthenticated user — mirrors the existing `/checkout` sign-in-gate test's structure. Also corrected the stale in-code comment above the checkout-gate tests, which claimed "No Clerk test-auth fixture exists in this repo" (false since 06-01) — now states a real fixture exists in the `regression` project but this suite deliberately stays unauthenticated-only per Phase 6's Scope. No deviations. Both ACs passed; `npx playwright test --project=smoke tests/e2e/smoke/smoke.spec.ts` 11/11 passing (10 pre-existing + 1 new). `06-06-SUMMARY.md` created, loop closed via unify. **Phase 6 is now 6/6 plans complete** — transitioned: ROADMAP.md marked Phase 6 ✅ Complete, STATE.md Current Position moved to Phase 7 (not started), `coder.json`'s `phase.number` set to 7.

### 06-06 planning started (2026-09-04): smoke-suite gap fill
During Phase 6 close-out review, user asked whether the existing unauthenticated smoke suite (`tests/e2e/smoke/smoke.spec.ts`) is complete. Found: `/bookings` (My Bookings) has zero smoke coverage — no reachability/gate assertion at all, unlike every other route group (`/`, `/search`, `/checkout`, `/confirmation/[id]`, `/sign-in`, `/sign-up`). Also noted (minor, non-blocking): the file's in-code comment claiming "No Clerk test-auth fixture exists in this repo" is now stale at the project level (06-01 built one) — smoke itself deliberately stays unauthenticated-only per ROADMAP.md's Phase 6 Scope, so this doesn't change smoke's scope, just needs the comment corrected. User agreed to reopen Phase 6 rather than defer to Phase 7 — added `06-06` to ROADMAP.md, reverted Phase 6/Overview status to in-progress in ROADMAP.md and `coder.json` (`phase.number: 6`, `loop.plan: "06-06"`, `loop.position: "PLAN"`). PROJECT.md's "Validated (Shipped)" regression-coverage notes for UC1/UC2/UC3/UC6 remain accurate (06-06 is smoke-only, doesn't touch regression). `06-06-PLAN.md` created (quick-fix track: 1 file, 1 task, no architectural implications) — adds an unauthenticated `/bookings` sign-in-gate smoke test mirroring the existing `/checkout` gate pattern, and corrects the stale "no Clerk test-auth fixture" comment. Awaiting APPLY approval.

### 06-05 applied+unified (2026-09-04): add-ons/UC6 + My Bookings regression specs — Phase 6 complete
Executed via `/coder:apply` on `06-05-PLAN.md`, the last plan in Phase 6. Built `tests/e2e/regression/addons.spec.ts` (2 specs: fee-bearing addon charge verified against a live `getAddOnsCatalog()`/`getWaivedAddOnIds()` read, and a perk-waived addon locked at $0 in the UI + rejected from `chargedAddonIds` on a direct API call) and `tests/e2e/regression/my-bookings.spec.ts` (2 specs: listing + Modify/Cancel/Addons entry-point wiring, and status-filter narrowing/clearing). One deviation, required to make AC-2 reachable at all: `searchInventory()`'s anonymous path always resolves each vendor's lowest-rank ("Gold Star") `NegotiatedTerm`, and per the seed data, Gold Star terms never carry any of the four addon-waiving perks (those exist only on `executive`/`business` tier terms) — confirmed via direct inspection of `negotiated_terms.json` and a temporary debug script, so no booking created through the real search+checkout path can ever land in the AC-2 scenario. Fixed by creating a normal booking, then directly patching its persisted `pricingSnapshot.perkIds` in Mongo to a real waiving perk (probed live via `getWaivedAddOnIds()` against the known seed vocabulary) — fixture manipulation, not a change to the boundary-protected `addons/route.ts`/`bookings/page.tsx`, same pattern as 06-03's direct-Mongo conflict seeding. Also fixed a card-locator issue: `CardTitle` renders a combined text node (`"{class_name} — {vendorId}"`), so exact-text matching never matched — switched to `[data-slot="card"]` + `hasText` substring matching. All 3 ACs passed; `tsc --noEmit` clean; isolated run 5/5; full `npx playwright test --project=regression` (17 specs) all passing (one pre-existing unrelated `modify.spec.ts` availability-race flake self-resolved on retry, not caused by this plan); no leftover Mongo fixtures; `git status` clean. `06-05-SUMMARY.md` created, loop closed via unify. **Phase 6 is now 5/5 plans complete** — transitioned per `transition-phase.md`: ROADMAP.md marked Phase 6 ✅ Complete, PROJECT.md's Validated section updated (UC1/UC2/UC3/UC6 all now regression-covered), STATE.md Current Position moved to Phase 7 (not started).

### 06-04 applied+unified (2026-09-04): cancellation/UC3 + concurrency race guards
Executed via `/coder:apply` on `06-04-PLAN.md`. Built `tests/e2e/regression/cancel.spec.ts` (1 spec: refund preview/confirm verified against a live `quoteCancellation()` read, not a hardcoded percentage; asserts `cancellation.refundAmountCents`/`refundPercent` in Mongo) and `tests/e2e/regression/concurrency.spec.ts` (2 specs, both bypass the UI via direct `request.post` — documented inline as a deliberate exception since a true race can't be reproduced through UI clicks): double-cancel (two concurrent `POST /cancel` → exactly one 200 + one 400, final status always `'cancelled'`, never reverted — the exact regression 04-08's atomic-claim fix targets) and concurrent-modify (two concurrent `POST /modify` targeting different cross-vendor inventory → exactly one 200 + one 409, exactly one `modificationHistory` entry, no merged state). One deviation: the plan's "different inventory" guidance for the concurrent-modify race wasn't sufficient on its own — an arbitrary cross-vendor candidate can be pricier than the original booking, and `modify/route.ts` requires a `paymentIntentId` for any `deltaCents > 0` *before* the CAS write runs, so two racing requests that both need payment they don't have would both 400 without ever exercising the race guard; fixed by adding `findTwoSamePricedCrossVendorCandidates()`, which only picks candidates matching the original booking's exact `dailyRate` (`deltaCents === 0` for both), guaranteeing both requests reach the CAS write unconditionally. All 3 ACs passed; `tsc --noEmit` clean; `cancel.spec.ts` 1/1; `concurrency.spec.ts` 3/3 consecutive full runs (per plan's "3+ consecutive runs" requirement); full `npm run test:e2e:regression` (12 specs) 12/12 passing; no leftover Mongo fixtures; `git status` clean. `06-04-SUMMARY.md` created, loop closed via unify. 06-05 (`depends_on: ["06-01"]`) remains unblocked for APPLY — the last plan in Phase 6.

### 06-03 applied+unified (2026-09-04): modification/UC2 regression specs
Executed via `/coder:apply` on `06-03-PLAN.md`. Built `tests/e2e/regression/modify.spec.ts` (5 specs, all against `/api/bookings/[id]/modify` directly via `request.post`, real Mongo + Stripe test mode): dates-increase (dry-run quote → real confirmed Stripe PaymentIntent for `deltaCents` → apply, verifies `pricingSnapshot.totalPrice`/`modificationHistory`), dates-decrease (auto-refund, verifies `refundAmountCents` matches `|deltaCents|`), cross-vendor success (delta-aware — covers a real PaymentIntent only if the new vendor is pricier), cross-vendor 409 (seeds a real conflicting `reserved` Booking against the exact target inventory/date window after confirming availability first), cross-vendor cutoff 400 (scans for a vendor with `modification_cutoff_hours` configured, read live via `getVendorPolicy()` — never hardcoded — picks a pick-up time inside that window on a genuinely-available target). Also added `tests/e2e/fixtures/booking-helpers.ts` (new, non-spec module). Four deviations, all required to make the specs correct/runnable: (1) the plan assumed `checkout.spec.ts` already exported a `createTestBooking(page)` helper — it didn't (06-02 only had inline logic) — fixed by extracting shared helpers into a plain fixtures module rather than a spec file, since Playwright hard-errors ("test file should not import test file") the moment two spec files import each other in the same run — this only surfaces under the combined suite, not per-file runs, so it wasn't visible until `npm run test:e2e:regression` was run with both new files present; (2) the plan's Task 1 UI/`mockStripePayment` instruction re-hit 06-02's exact server-side-`retrieve()` wall (`modify/route.ts` line 127) — fixed identically to 06-02, with all 5 specs calling the API directly and using real Stripe test-mode PaymentIntents for any positive delta; (3) reading `modify/route.ts` in full showed `checkModificationCutoff()` only fires on the `isInventoryOrVendorChange` branch — the cutoff-400 spec had to be built as a cross-vendor change, not dates-only, contradicting an assumption implied by the plan's AC grouping; (4) the cross-vendor-success spec had to handle either delta sign (dry-run first, only pay if `deltaCents > 0`) since the plan didn't pin the target vendor's relative price. All 4 ACs passed; `tsc --noEmit` clean; `npm run test:e2e:regression` (9 specs total) 8/9 in one run — `payment-smoke`'s pre-existing `@payment-smoke` spec (from 06-02) failed only under 5-way worker parallelism and passed cleanly in isolation, confirmed not a regression from this plan's changes; `modify.spec.ts` alone 5/5; no leftover seeded documents (every spec cleans up via `finally`, including the seeded conflict booking); `git status` clean of stray files. `06-03-SUMMARY.md` created, loop closed via unify. 06-04/06-05 (`depends_on: ["06-01"]`) remain unblocked for APPLY.

### 06-02 applied+unified (2026-09-04): checkout/UC1 regression specs + real Stripe Elements smoke
Executed via `/coder:apply` on `06-02-PLAN.md`. Built `tests/e2e/regression/checkout.spec.ts` (happy-path booking creation + tampered-Stripe-amount rejection) and `tests/e2e/regression/payment-smoke.spec.ts` (`@payment-smoke`-tagged real Stripe Elements iframe test). Three deviations from plan, all required to make the specs actually exercise the real code paths: (1) the plan's Task 1 instructed mocking Stripe's response via `page.route()` so the server's `stripeAPI.paymentIntents.retrieve()` call would see a reduced amount — architecturally impossible, since Playwright's browser-level interception cannot affect the Next.js server process's own outbound Stripe API call; fixed by creating real, already-`succeeded` Stripe test-mode PaymentIntents directly via the Stripe Node SDK (`pm_card_visa`, `confirm: true`) for whatever amount each spec needs, then submitting that real PaymentIntent id to `/api/bookings` via `request.post`. (2) 06-01's `testMember`/`testBooking` fixtures are unusable here — `POST /api/bookings` always ties the created Booking to the real signed-in Clerk session's member via `getOrCreateMember`, never to any client-supplied member — fixed by tracking/cleaning up bookings directly via `bookingId`/`paymentIntentId` against the real `Booking` model, using only the `mongoClient` fixture. (3) `payment-smoke.spec.ts`'s real Stripe Elements DOM structure had to be found via live inspection (not in source): the PaymentElement's "Card" tab picker and the resulting card-number/expiry/CVC fields both render inside the `elements-inner-accessory-target` iframe, not `elements-inner-easel` as initially assumed — found by querying `frame.getByRole('button', {name:'Card'})` across all `page.frames()`. All 3 ACs passed; `tsc --noEmit` clean; `npm run test:e2e:regression` 4/4 (setup + both checkout specs + payment-smoke); `--grep @payment-smoke` passes in isolation; no leftover test data or scratch files. `06-02-SUMMARY.md` created, loop closed via unify. 06-03/06-04/06-05 (`depends_on: ["06-01"]`) remain unblocked for APPLY.

### 06-01 applied+unified (2026-09-04): fixture foundation complete
Executed via `/coder:apply 06-01` after explicit user approval. Task 1 (Playwright project restructuring: `setup`/`smoke`/`regression`, `@clerk/testing` install, `smoke.spec.ts` relocation, npm scripts, `.env.test.example`) and Task 2 (`auth.setup.ts`, `test-data.ts`, `stripe-mock.ts`) both built and qualified. Two real bugs found and fixed during live checkpoint verification, neither anticipated by the plan: (1) Clerk's real sign-in UI has a "Show password" toggle button that also matches `getByLabel('Password')` — a strict-mode locator collision, fixed with `{ exact: true }`; (2) the scripted flow's `page.goto('/')` raced Clerk-JS's own session-cookie write, capturing a signed-out `storageState` — fixed by waiting for the `attempt_first_factor` response + `networkidle` before navigating. Also surfaced a **Clerk-dashboard-level** issue outside code: the test instance had a "device/client-trust verification" (new-device gate) setting enabled, causing password-verified sign-ins to return `needs_client_trust`/`created_session_id: null` — user disabled it in the dashboard, not a code fix. `test-data.ts`'s `base.extend<TestFixtures, WorkerFixtures>()` single-call form left `use`/`testInfo` as implicit-`any` under this Playwright/TS version; fixed by splitting into two chained `.extend()` calls with explicit param types. User created `.env.test` with a real Clerk test-instance user (`playwright1+clerk_test@example.com`) themselves, as agreed. All 4 ACs passed; `tsc --noEmit` clean; `npm run test:e2e:smoke` 10/10; `npx playwright test --project=setup` captures a genuine `__session`-cookie storageState; `npm run test:e2e:regression` runs clean (0 regression specs yet, expected — 06-02..06-05 add those). `06-01-SUMMARY.md` created, loop closed via unify. 06-02 through 06-05 (`depends_on: ["06-01"]`) are now unblocked for APPLY.

### 06-02/06-03/06-04/06-05 planned (2026-09-04): full regression-spec coverage for Phase 6
Per explicit user request ("work on the complete plan 6-02 as well and any further plans for this phase before APPLY"), planned the rest of Phase 6 in one pass rather than one plan at a time — split the regression-spec scope into 4 vertical-slice plans by flow family (matching the `authenticated-flows-inventory.md` research), each `depends_on: ["06-01"]` since all consume 06-01's fixtures (`auth.setup.ts`'s storageState, `testMember`/`testBooking`, `mockStripePayment`) directly:
- **06-02** (checkout/UC1): happy-path booking creation + tampered-Stripe-amount server-side rejection (closes the "rate-integrity verified by code review only" gap) + one `@payment-smoke`-tagged real-Stripe-Elements test, isolated by tag so it can be excluded from fast runs later.
- **06-03** (modification/UC2): dates-only increase/decrease, cross-vendor success/409 (seeds a real conflicting reservation to force the rejection), modification-cutoff 400 (reads the real `VendorPolicy.modification_cutoff_hours` rather than hardcoding it) — explicitly the closure STATE.md's own Deferred Issues entry called for re: 04-13/04-14's code-trace-only verification.
- **06-04** (cancellation/UC3 + concurrency): refund preview/confirm against a live `getVendorPolicy()` read, plus two dedicated race-guard specs (double-cancel, concurrent-modify CAS) that bypass the UI and hit the API routes directly via `Promise.all` — the only way to reliably reproduce a true race, called out explicitly as a deliberate UI-bypass exception.
- **06-05** (add-ons/UC6 + My Bookings): fee-bearing addon charge and perk-waived lock/no-double-charge, both driven from a live `getAddOnsCatalog()`/`getWaivedAddOnIds()` read rather than hardcoded assumptions, plus a shallow My Bookings listing/entry-point-wiring spec (doesn't duplicate 06-02/06-03/06-04's submit-flow assertions).

No CI-wiring plan added — deliberately out of scope for every 06-0x plan (each plan's Boundaries says so); flagged as a possible future 06-06 once the specs themselves are proven, not before. All 4 plans instruct their implementer to read real route/component/schema files before writing assertions (request-body shapes, VendorPolicy field names, addon dialog copy) rather than guessing, continuing the pattern established in 06-01. None have been adversarially reviewed yet (not requested) and none applied — user's explicit ordering is "plan everything, then APPLY."

### 06-01 planned (2026-09-04): test infrastructure foundation
Ran `/coder:research-phase` (3 parallel agents: sibling-repo Playwright/Clerk pattern, this repo's authenticated-flow inventory, industry best practices — findings in `.coder/phases/06-regression-test-infrastructure/RESEARCH.md`), then `/coder:plan`. Classified complex (4+ subsystems: Clerk fixture, config restructuring, data fixtures, Stripe mocking) — split into `06-01` (fixture foundation only) + `06-02` (TBD, actual regression specs), per CODER sizing guidance rather than one oversized plan. `06-01-PLAN.md` (wave 1, no deps, 2 auto tasks + 1 blocking checkpoint): Task 1 installs `@clerk/testing`, moves `tests/e2e/smoke.spec.ts`→`tests/e2e/smoke/smoke.spec.ts` (unchanged content), restructures `playwright.config.ts` into `setup`/`smoke`/`regression` projects, adds `test:e2e:smoke`/`test:e2e:regression` scripts. Task 2 builds `auth.setup.ts` (real Clerk UI sign-in via `@clerk/testing`, persists `storageState`), Mongo test-data fixtures (worker-scoped client, tagged self-cleaning `testMember`/`testBooking`), and a Stripe network-mock helper stub (flags the "how does the UI treat a mocked client secret" question as unresolved, deferred to 06-02). Checkpoint requires a real Clerk test-instance user in `.env.test` and live verification that `storageState` captures a genuine signed-in session. Not yet applied.

### 04-13/04-14 adversarial review (2026-09-04)
`04-13-PLAN.md` (1 full review + 1 targeted spot-check, 2 agents total): found and fixed 6 issues — most severe (BLOCKING): Task 2 told the implementer to read a `vendorId` off `quoteModification()`'s return, but `ModificationQuote` has no such field — fixed to write the request's own validated `targetVendorId`. Also fixed: AC-4's CAS filter didn't guard on `inventoryId` (only `totalPrice`), missing a same-priced concurrent-swap case; `checkAvailability`/`checkModificationCutoff` take `Date` but the route only has ISO strings, no conversion instruction; `excludeBookingId` needed an explicit `String(booking._id)` cast; dates-only modifications silently skip the cutoff check with no disclosure (now an explicit accepted non-goal in Boundaries); `findEquivalentInventory()`'s param was wrongly named `vehicleClassId` (no such field — `VehicleClass` is keyed by `class_name`), fixed to `vehicleClassName` — the spot-check round caught one leftover stale reference to the old name still in Boundaries, fixed in a follow-up edit. Confirmed clean on final read-through.
`04-14-PLAN.md` (1 full review + 1 targeted spot-check, 2 agents total): found and fixed 5 issues — most severe (BLOCKING): the plan framed the picker as browsing to a different location/vehicle-type, but `findEquivalentInventory(locationLabel?, vehicleClassName?)` filters *to* the given location/class and can never surface a different one — fixed by narrowing scope throughout (Objective, Purpose, Output, AC-1, Task 1, Boundaries, checkpoint) to "alternate vendors/units at the booking's current location and vehicle class," with true location/class browsing explicitly deferred as a follow-on plan needing a new HTTP wrapper route. Also fixed: same `vehicleClassId`→`vehicleClassName` naming mistake (made independently here too); no per-booking dedup guidance for `bookings/page.tsx` rendering N bookings at potentially different locations/classes — fixed by requiring dedup on distinct `(location_label, class_name)` pairs, mirroring the page's existing `distinctPerkIdsKeys` pattern; Task 1's trigger condition wrongly said "both pages that render `BookingModifyDialog`" when the confirmation page renders `BookingModifyForm` directly (no dialog) — reworded; no instruction to exclude the booking's own current `inventoryId` from candidates despite AC-1 requiring it — added explicitly. Round-2 spot-check verified all 5 fixes PASS against actual source (`lib/graph/queries.ts`, both page files). Both plans used 2 of the standing max-3-agents/max-2-rounds budget each, run strictly sequentially per project convention — within cap, no further review round planned.

### 04-13/04-14 planned (2026-09-04): modify location/vehicle-type, API+UI split
Ran `/coder:research` (two parallel subagents — mechanics + platform/vendor-boundary angle), then `/coder:plan`. Research docs: `.coder/research/04-13-modify-location-vehicle-type.md` (availability-check + cross-vendor re-match mechanics) and `.coder/research/agentic-boundary-for-04-13.md` (applies `mastech-agentic-commerce`'s Store Ops Agent/A2A boundary pattern — live fulfillment/availability state must never be modeled in the shared Postgres+AGE graph, only candidate/structural data). Asked 4 clarifying questions via AskUserQuestion, all answered: UI scope → **Full UI + API** (drove the 04-13/04-14 split, since API and UI are separate subsystems per CODER sizing guidance); optimistic-lock (CAS) fix → **fold into 04-13** (retrofits `findOneAndUpdate` onto `modify/route.ts`'s pre-existing plain `.save()`, closing a real lost-update race flagged since 05-01); pending-status bookings as an availability conflict → **no, reserved/checked_in only**; modification-cutoff gate → **add now, 04-13-only** (new `VendorPolicy.modification_cutoff_hours` check specific to location/vehicle-type changes, reusing the existing policy lookup). `04-13-PLAN.md` (wave 10, depends_on 04-07): `checkAvailability()`/`checkModificationCutoff()` in `lib/vendor-integration/policy.ts`, `findEquivalentInventory()` in `lib/graph/queries.ts`, extended `modify/route.ts` (availability → cutoff → cross-vendor quote → CAS write), 2 auto tasks + 1 blocking checkpoint. `04-14-PLAN.md` (wave 11, depends_on 04-13): picker UI in `booking-modify-form.tsx`/`booking-modify-dialog.tsx`, wired into the existing dry-run flow, 2 auto tasks + 1 blocking checkpoint. Neither plan has been adversarially reviewed yet (not requested); neither has been approved/applied.

### Phase 5 closed (2026-09-03)
05-01 (standalone "Manage extras" add-on management) applied and unified: `POST /api/bookings/[id]/addons` (optimistic-lock `findOneAndUpdate` write, reuses 04-11/04-12's waiver helpers, Stripe delta-payment/refund patterns from 04-07/04-08) + `BookingAddonsDialog`/`Form` wired into both the confirmation page and My Bookings. All 5 ACs passed; `tsc`/`build` clean; `npm run test:e2e` 10/10; live checkpoint approved by user. `05-01-SUMMARY.md` created. Phase 4 (04-00–04-12) and Phase 5 (05-01) are both now complete — see ROADMAP.md. 04-13 (modify location/vehicle-type) remains separately TBD/roadmap-only, not blocking Phase 4/5 completion.
Plan: 04-11 applied and unified (vehicle-card enrichment + supplier filter); 04-12 (checkout enrichment + extras toggles + rate-integrity extension, depends_on 04-11) applied and unified — checkpoint approved-in-substance, 04-12-SUMMARY.md created/amended 2026-09-02. Per user request, 04-12's own scope was expanded post-checkpoint (rather than split into a new plan) to also cover: pickup/drop-off location validation on hero+filters+vehicle-type modal, `/search` gate-on-missing-location, the vehicle-type-carousel search modal, "All Vehicles" label+value fix, and checkout's "Back to results" nav with `returnTo` state preservation. 04-12-PLAN.md amended in-place (Tasks 4-7 added, files_modified/AC/boundaries/checkpoint/verification updated) to match what was actually built and unified.
Status: 04-10 (vehicle-class variety, richer images, carousel/marquee polish, Playwright smoke suite) was fully executed this session; its blocking human-verify checkpoint was presented to the user, who then pivoted directly to the new card/filter/checkout request without typing "approved" — treated as approved-in-substance (user reviewed the live result and moved forward rather than raising issues) but noted here since it never received the literal confirmation the checkpoint format expects. 04-06 (landing-page rework), 04-07 (modification/UC2), 04-08 (cancellation+refund/UC3), and 04-09 (hero/search alignment + filter parity + vehicle-card images, including its Task 9 addendum) have all been applied — see Prior activity below for 04-06 detail; 04-07/04-08/04-09 summaries not reproduced here but confirmed present on disk. 04-00 applied (landing/search/vehicle/checkout screens + header + polish pass built and verified live, checkpoint approved) but never unified (no 04-00-SUMMARY.md yet — /coder:unify not run per no-unify-without-request constraint). 04-01 (MUI re-theme) applied and unified: `app/globals.css` re-themed (5 :root tokens), all 3 ACs passed, no deviations. 04-02 (real images + My Bookings + landing rework + location autocomplete) executed ad hoc via plan-mode, retroactively documented in 04-02-SUMMARY.md. 04-03 (search-results BookCars-style layout: sidebar + horizontal cards, mock-data fuelType/doors/hasAC/rating/tripCount fields with rating reused from real Vendor.rating) executed via /coder:apply, checkpoint approved, unified via /coder:unify: ROADMAP.md's Phase 4 sequencing resequenced — real discovery/search is now 04-04, checkout+booking/UC1 is 04-05, modification/UC2 is 04-06, cancellation+refund/UC3 is 04-07. UC4/5/7/8 mock screens explicitly deferred by user. 04-04 (real Discovery/Search UI) applied via /coder:apply 2026-08-28, passed a 2-round adversarial plan review before APPLY: added `searchInventory(locationLabel?)` in `lib/graph/queries.ts` (resolves each vendor's lowest-rank-MembershipTier NegotiatedTerm + perks, one row per Inventory item — verified live against the seeded graph: 1000 unfiltered rows, 100 for a location filter, 0 rows missing a negotiatedTerm), extended `runCypher()` in `lib/graph/client.ts` with an optional `columns` param (multi-column Cypher RETURN needs an explicit AGE column list — the existing single-column wrapper couldn't express `RETURN i, vc, v, l`; this is additive/backward-compatible, the 3 existing single-column callers are untouched), added `components/vehicle-card.tsx` and `app/(discovery)/search/page.tsx` (async server component, awaits `searchParams`, plain GET `<form>` for location/vehicle-class filters). `npm run build` clean; `curl localhost:3000/search` → 200 with real vendor data (Alamo); `curl localhost:3000/prototype/search` → 200 unchanged. All 3 ACs passed. Post-APPLY, user manual testing caught a real bug (sidebar filter form had no submit button) — fixed, rebuilt clean, re-verified live. Landing→/search entry point explicitly deferred to 04-05 per user decision. Unified via /coder:unify 2026-08-28: 04-04-SUMMARY.md created, loop closed.
Last activity: 2026-09-02 — Closed 04-12's loop via /coder:unify, then amended it in-place per user request ("update the 04-12 plan with location validation, vehicle-type search modal, all vehicles labeling, back to results") to fold in 4 additional tasks (Task 4: search-entry location validation; Task 5: /search gate + vehicle-type modal; Task 6: "All Vehicles" labeling fix; Task 7: back-to-results nav) and 4 additional ACs (AC-4 through AC-7), all already implemented and verified live earlier in the session. Updated `04-12-PLAN.md`'s frontmatter (`files_modified` now includes `app/landing-hero.tsx`, `components/search-form-fields.tsx`, `components/search-filters.tsx`, `components/vehicle-type-carousel.tsx`, `app/(discovery)/search/page.tsx`, `components/vehicle-card.tsx`), objective/output, acceptance criteria, boundaries (noted the superseded DO-NOT-CHANGE on 3 files), checkpoint `<what-built>`/`<how-to-verify>`, and the verification checklist. Rewrote `04-12-SUMMARY.md` to document all 7 ACs as Pass, including a real coverage gap found+fixed during Task 7's live verification (back link initially missing from the sign-in-required checkout branch) and 2 smoke-test regressions from Tasks 5/6 (carousel `<Link>`→`<button>`; `/search` gate). `npm run build` clean; `npm run test:e2e` 10/10 passing. No new 04-13 plan needed — this work now lives entirely inside 04-12, loop re-closed.

Prior activity (2026-08-31): User said "go ahead and APPLY" (both 04-11/04-12 had already passed the standing adversarial review and been presented). Executed 04-11-PLAN.md via /coder:apply: Task 1 wrote `scripts/add-vehicle-extras-fields.mjs` (deterministic `fuel_policy`/`deposit_amount` assignment, pattern from `reassign-vehicle-makes.mjs`), manually added `fee_per_day` to 5 addon_catalog.json entries, ran the script (1000 rows updated), reloaded the graph (`load_seed_data.py`), updated VOCABULARY.md/ONTOLOGY.md, verified via direct Cypher (1000/1000 rows have both new fields; AddOn fee_per_day correct on all 5+3 entries). Task 2 added `getVendorPolicy`/`getAddOnsCatalog`/`getWaivedAddOnIds` to `lib/graph/queries.ts` — live verification caught and fixed a real bug: `RETURN a.addon_id AS addonId` + `columns: ['addonId']` silently returned `undefined` because Postgres folds the unquoted camelCase alias to lowercase; fixed by using `addon_id` throughout. Task 3 added the chip row (powertrain/fuel policy/mileage/cancellation window/deposit) + `border-l-4` brand accent to `vehicle-card.tsx`, a submit-driven Suppliers checkbox filter to `search-filters.tsx`, and `providers` URL-param filtering to `search/page.tsx` — verified via `npm run build` (clean) and live curl of rendered HTML (all chips present; `providers=Hertz` correctly narrows 1000→100 results). `npm run test:e2e` 7/7 passing, no regressions. All 3 ACs passed, one auto-fixed bug (see 04-11-SUMMARY.md), no deviations otherwise. Unified: `04-11-SUMMARY.md` created.

Prior activity: 2026-08-28 — Applied 04-05-PLAN.md via /coder:apply (adversarially reviewed before APPLY; see prior entry for the review's fixes). Task 1: rewired `app/page.tsx` (real search form) and `components/vehicle-card.tsx` ("Choose this car" now a real `Link` to `/checkout`). Task 2: added `lib/graph/mutations.ts` (`recordReservation()`, MERGE-based, parameterized — live-verified idempotent: 2 calls → 1 Member node, 2 distinct RESERVED edges as intended), `lib/models/member-sync.ts` (`getOrCreateMember()`), and `app/api/bookings/route.ts` (requires `auth()`, re-derives pricing server-side via `searchInventory()`, rejects on Stripe-amount mismatch, Clerk-sourced identity). Task 3: installed `@stripe/stripe-js`/`@stripe/react-stripe-js`; added `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` to `.env.local` (user-provided) and documented both Stripe env vars with comments in `.env.local.example`; built `app/(checkout)/checkout/page.tsx` + `checkout-form.tsx` (real Stripe Elements, fixed `today+14`–`+17` dates, server-computed total). Task 4: built `app/(checkout)/confirmation/[bookingId]/page.tsx` (real Booking lookup, ObjectId-validated, shows vehicle class/vendor via `searchInventory()` join per spec). Checkpoint (blocking): first attempt failed — "Continue to payment" 500'd with Stripe `email_invalid` because the checkout page had no sign-in gate and passed an empty email; classified as a Spec gap in Task 3, fixed by adding the same sign-in-gate pattern used in `app/prototype/bookings/page.tsx`. Re-tested end-to-end and approved by user; bookingId `6a918a636e134d1cdc979024` verified in both Mongo (`status: reserved`, correct `pricingSnapshot`) and the graph (1 Member node, 1 RESERVED edge to RC10001). All 3 ACs passed, no open deviations. Mid-checkpoint the user also flagged that the real landing page doesn't match the `/prototype/landing` design (hero photo, why-us grid, vendor list, browse-by-vehicle-type) — classified as a Spec gap in 04-05's Task 1 (intentionally scoped to wiring only), not a defect; user chose to defer this to a **new dedicated plan** (real-data port of the prototype landing, not mock data) rather than expand this checkpoint. Unified via /coder:unify 2026-08-28: 04-05-SUMMARY.md created, loop closed.

Progress:
- Milestone: [██████░░░░] 58% (7 of 12 phases complete — expanded from 7 to 12 phases 2026-09-05)
- Phase 1: [██████████] 100%
- Phase 2: [██████████] 100%
- Phase 3: [██████████] 100% (03-01, 03-02, 03-03 all complete)
- Phase 4: [██████████] 100% (04-00 applied+unified; 04-01 applied+unified; 04-02 applied ad hoc, documented; 04-03 applied+unified via full loop; 04-04 applied+unified via full loop; 04-05 applied+unified via full loop; 04-06 applied+unified; 04-07 applied+unified 2026-09-03 (modification/UC2 + My Bookings, 4 live-testing fixes folded in); 04-08 applied+unified 2026-09-03 (cancellation+refund/UC3 + decreasing-modification refund fold-in); 04-09 applied+unified incl. Task 9 addendum; 04-10 applied+unified; 04-11 applied+unified; 04-12 applied+unified, scope expanded in-place to include search-entry gating/modal/labeling/back-nav; 04-13 applied+unified 2026-09-04 (modify location/vehicle-type API); 04-14 applied+unified 2026-09-04 (modify location/vehicle-type UI). Phase 4 now fully complete.)
- Phase 5: [██████████] 100% (05-01 applied+unified 2026-09-03 — standalone add-on management)
- Phase 6: [██████████] 100% (06-01 applied+unified — fixture foundation; 06-02 applied+unified 2026-09-04 — checkout/UC1 regression + payment smoke; 06-03 applied+unified 2026-09-04 — modification/UC2 regression; 06-04 applied+unified 2026-09-04 — cancellation/UC3 + concurrency race guards; 06-05 applied+unified 2026-09-04 — add-ons/UC6 + My Bookings; 06-06 applied+unified 2026-09-04 — smoke-suite gap fill (`/bookings` coverage + stale-comment fix). Phase 6 fully complete, 6/6 plans.)
- Phase 7: [██████████] 100% (07-01 applied+unified 2026-09-05 — EquivalenceCluster/PART_OF_CLUSTER substitution relation; 07-02 applied+unified 2026-09-07 — Azure OpenAI Location-synonym backfill + agent-lab HTML ontology/knowledge-graph viewer + Member-drift cleanup; 07-03 applied+unified 2026-09-07 — composable retriever tools (`lib/graph/retrievers.ts`). Phase 7 fully complete, 3/3 plans.)
- Phase 8: [██████████] 100% (08-01 applied+unified 2026-09-08 — Vendor Agent A2A layer at agent-service/vendor-agent/. Phase 8 fully complete, 1/1 plans.)
- Phase 9: [██████████] 100% (09-01 applied+unified 2026-09-08 — Customer Assistant backend, 10-tool /chat SSE endpoint; 09-02 applied+unified 2026-09-08 — chat launcher/panel UI, 2 real bugs auto-fixed (CORS gap, Confirm-button race condition); 09-03 applied+unified 2026-09-08 — conversational UX upgrade (hybrid search, VehicleCard rendering, auto-scroll), 4 real issues auto-fixed; 09-07 applied+unified 2026-09-15 — `/agentic-travels` basePath routing; 09-08 applied+unified 2026-09-15 — get_booking_status sort fix + bookingId/pickupDate/city/vendorId server-side filters. Phase 9 fully complete, 5/5 plans (reopened for 09-03, 09-06, then 09-07/09-08, reclosed each time).)
- Phase 10: [░░░░░░░░░░] 0% (not started — Agent Lab & Observability/Evals, new 2026-09-05)
- Phase 11: [░░░░░░░░░░] 0% (not started — Unified Agent-Service Deployment, new 2026-09-05)
- Phase 12: [░░░░░░░░░░] 0% (not started — Vendor Fulfillment Touchpoints, renumbered from Phase 7 2026-09-05)

## Loop Position

Current loop state (04-00 / 04-06 / 04-09 / 04-10):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [all 4 retroactively unified 2026-09-02 — code was already applied/committed, SUMMARY.md had never been written]
```

Current loop state (04-11):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-11 complete: 04-11-SUMMARY.md created, loop closed]
```

Current loop state (04-12):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-12 complete: 04-12-SUMMARY.md created, loop closed]
```

Current loop state (04-07):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-07 complete: checkpoint closed 2026-09-03 (user closed the loop after 3 rounds of live-testing fixes + a nav-consistency fix), 04-07-SUMMARY.md created.]
```

Current loop state (04-08):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-08 complete: all 7 auto tasks + checkpoint approved 2026-09-03, 04-08-SUMMARY.md created.]
```

Current loop state (05-01):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [05-01 complete: both auto tasks + checkpoint approved 2026-09-03, 05-01-SUMMARY.md created. Phase 5 closed.]
```

Current loop state (04-13):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-13 complete 2026-09-04: both auto tasks done, checkpoint approved, 04-13-SUMMARY.md created. Proceeding to 04-14 APPLY.]
```

Current loop state (04-14):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-14 complete 2026-09-04: both auto tasks done, checkpoint approved twice (once before, once after a post-checkpoint candidate-label fix), 04-14-SUMMARY.md created. Phase 4 fully closed.]
```

Current loop state (06-02):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [06-02 complete 2026-09-04: both auto tasks done, no checkpoint (autonomous: false but no blocking checkpoint task defined), 06-02-SUMMARY.md created. 06-03/06-04/06-05 remain planned, awaiting APPLY.]
```

Current loop state (06-04):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [06-04 complete 2026-09-04: cancellation/UC3 + concurrency race guards, both specs (cancel.spec.ts, concurrency.spec.ts) built and verified (12/12 full regression suite, 3/3 consecutive concurrency runs), 06-04-SUMMARY.md created.]
```

Current loop state (06-05):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [06-05 complete 2026-09-04: add-ons/UC6 + My Bookings (addons.spec.ts, my-bookings.spec.ts), both built and verified (17/17 full regression suite), 06-05-SUMMARY.md created.]
```

Current loop state (06-06):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [06-06 complete 2026-09-04: smoke-suite gap fill (`/bookings` sign-in-gate test + stale Clerk-fixture comment correction), verified via `npx playwright test --project=smoke` 11/11, 06-06-SUMMARY.md created. Phase 6 (6/6 plans) fully complete — transitioned to Phase 7.]
```

Current loop state (07-01):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [07-01 complete: all 3 auto tasks done, 07-01-SUMMARY.md created. Not last plan in Phase 7 — 07-02/07-03 remain TBD, no transition triggered.]
```

Current loop state (07-02):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [07-02 complete 2026-09-07: 4 tasks (incl. mid-APPLY Task 4 addition) + post-APPLY drift cleanup/knowledge-graph expansion, all 3 ACs Pass, 07-02-SUMMARY.md created. Not last plan in Phase 7 — 07-03 remains TBD, no transition triggered.]
```

Current loop state (07-03):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [07-03 complete 2026-09-07: composable retriever tools (lookupNode/traverse/resolveSynonym), all 4 ACs Pass, 07-03-SUMMARY.md created. Last plan in Phase 7 (3/3) — transition to Phase 8 triggered.]
```

Current loop state (09-02):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [09-02 complete 2026-09-08: chat launcher/panel UI, 2 real bugs auto-fixed (CORS gap in 09-01's server.ts, Confirm-button race condition), all 4 ACs live-verified incl. direct MongoDB proof of AC-3's persisted modification. 09-02-SUMMARY.md created. Last plan in Phase 9 (2/2) — transitioned to Phase 10.]
```

Current loop state (09-03):
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [09-03 complete 2026-09-08: conversational UX upgrade — hybrid (structured+synonym) search_inventory filter, conversational one-sentence SYSTEM_PROMPT, VehicleCard-based result rendering, auto-scroll. 4 issues auto-fixed during live verification (model tool-argument omission, verbose card-restating prose, narrow-panel VehicleCard truncation, missing auto-scroll) — the last two found via direct Playwright screenshot review, not assertions alone. All 4 ACs Pass. 09-03-SUMMARY.md created. Phase 9 reopened (per 06-06 precedent) then reclosed at 3/3 plans — re-transitioned to Phase 10.]
```

### 04-07 closed (2026-09-03)
All 4 auto tasks executed and qualified (build/tsc clean, `npm run test:e2e` 10/10). Task 5's blocking human-verify checkpoint surfaced 4 real live-testing gaps, all fixed and folded into `04-07-PLAN.md` before closing: (1) no success confirmation after a modification — added a `successMessage` banner; (2) live quote required clicking away from the date input — replaced `onBlur` with a debounced `useEffect`; (3) decrease-path copy read as unfriendly — reworded, with an explicit (now superseded-by-04-08) refund disclosure; (4) confirmation page's back-navigation was inconsistent with the rest of the app (bottom "Back to search" vs. checkout's top-left "Back to results") — standardized to a top-left "← Back to my bookings" link routing to `/bookings`. `04-07-PLAN.md` amended in-place (Task 3 action/verify, checkpoint `<what-built>`, verification checklist) to document all 4 fixes as built, not just planned. `04-07-SUMMARY.md` created — loop closed via `/coder:unify`.

### 04-08 closed (2026-09-03)
All 7 auto tasks executed and qualified (build/tsc clean, `npm run test:e2e` 10/10). Added a real per-vendor `no_show_fee_percent` to all 10 seeded `VendorPolicy` nodes and reloaded the graph; added `quoteCancellation()` to `lib/vendor-integration/policy.ts`; built `POST /api/bookings/[id]/cancel` with an atomic status-claim before the Stripe refund (closes the concurrent-double-cancel race); added `BookingCancelDialog` to both the confirmation page and My Bookings; closed 04-07's decreasing-modification refund gap (Task 7/AC-8) in `modify/route.ts` and `booking-modify-form.tsx`. One non-structural correction found live: `PaymentIntent.amount_refunded` isn't a real field — both refund paths now expand `latest_charge` to read it off the `Charge`. Checkpoint approved by user; `04-08-SUMMARY.md` created — loop closed via `/coder:unify`.

### 04-08 amended (2026-09-03): folded in decreasing-modification refund
During 04-07's live checkpoint testing, the user asked how refunds are handled for a decreasing modification (04-07's UI, added live, disclosed it as "not issued automatically — contact support"). Rather than leave it as a permanent gap or spin up a new plan, user chose to fold it into 04-08 (which already builds the Stripe-refund machinery for cancellation). Added: AC-8, Task 7 (`app/api/bookings/[id]/modify/route.ts` — when `deltaCents < 0` on a real apply, issue `stripe.refunds.create` for `-deltaCents` against the original PaymentIntent, idempotency-keyed on `modify-refund-{bookingId}-{historyLength}`; record `refundId`/`refundAmountCents` on the `modificationHistory` entry; update `components/booking-modify-form.tsx`'s copy to confirm the refunded amount instead of "contact support"). `lib/models/Booking.ts`'s `modificationHistory` subdocument widened with the same two optional fields. This is the one place 04-08 now genuinely depends on 04-07 rather than being symmetric/order-independent — `depends_on` updated to `["04-05", "04-07"]`, `wave` bumped 8→9. Not yet adversarially reviewed as a standalone round (single, contained addition — reusing an already-reviewed refund pattern from Task 4); flag for a light spot-check before APPLY if a fresh review pass is warranted.

### Roadmap addition (2026-09-03): 04-13 TBD — modify location/vehicle-type
During 04-07's live checkpoint testing, user asked whether modifying location/vehicle-type (not just dates) makes sense. Assessed and added as a new, separate future plan rather than folding into 04-07/04-08: it introduces two concerns those plans deliberately don't handle — an availability/double-booking check against the target inventory (no such signal exists in the ontology yet — flagged as needing research), and a full re-match against a potentially different vendor (not just a re-quote of the same inventoryId/vendorId pair), including that vendor's distinct perk set. `modificationHistory` (added by 04-07) already stores `inventoryId`/`vendorId` per entry, so no schema change is anticipated. See ROADMAP.md Phase 4's new 04-13 entry.

### Roadmap expansion (2026-09-05): Agent Service phases inserted, Phase 7 renumbered to 12
Per user request ("extend the roadmap with Agent Service... push phase 7 and renumber"), ran `/coder:research` (2 parallel agents: `Explore` against `mastech-agentic-commerce`'s Store Ops Agent/A2A pattern, storefront chat agent, Agent Lab, Phoenix, docker-compose, health scripts; `general-purpose` for 2025-2026 web best practices on conversational booking agents, A2A protocol, GraphRAG-style ontology enrichment, Agent Lab tooling, Phoenix evals) — findings in `.coder/research/agent-service-extension.md`. Then used `/coder:add-phase`'s underlying workflow manually (the skill's own process only supports appending one phase at a time; this needed inserting 5 phases before an existing one and renumbering it, so ROADMAP.md/directories were edited directly following the same conventions) to insert:
- **Phase 7** — Agent-Grounding Ontology & Knowledge-Graph Enrichment (thesaurus/synonym backfill for new vocab, new substitution/equivalence-cluster-style relation, multi-tool graph retrieval surface) — foundation both new agents consume
- **Phase 8** — Vendor Agent (A2A Inventory/Modification/Cancellation) — separate process/service, static Agent Card + JSON-RPC `/a2a`, mirrors commerce's Store Ops Agent; flags 2 decisions needed before planning (A2A conformance depth, per-vendor deployment topology) and the explicit gap vs. commerce's binary-in-stock model (needs date-range availability)
- **Phase 9** — Customer/Driver Assistant Agent (discovery-to-checkout tool chain wrapping existing API routes, session state outside the LLM, proposal→confirm human-in-the-loop pattern enforced at the orchestration layer — a deliberate strengthening over commerce's detect-after-stream-only guardrail, justified by this project's rate/perk-integrity Core Value)
- **Phase 10** — Agent Lab & Observability/Evals (Phoenix) — flags that commerce's own "Agent Lab" is Jupyter-notebook-only KG exploration, not prompt/tool-testing tooling, so this phase's actual scope (richer dev tool vs. lighter notebook) needs an explicit decision before planning
- **Phase 11** — Unified Agent-Service Deployment — single docker-compose across all new + existing services, health-check/start/stop script trio ported from commerce

The former Phase 7 (Vendor Fulfillment Touchpoints, UC4/5/7/8) is renumbered to **Phase 12**, unchanged in content except a note that UC5 (in-rental support) is a natural fit for Phase 9's assistant if pulled forward. No plans created for any of the 5 new phases yet (all `TBD`, `/coder:plan` not run). Phase directories created: `.coder/phases/07-agent-grounding-ontology-enrichment/` through `11-unified-agent-service-deployment/` (old Phase 7 had no directory yet, so no rename/conflict). Milestone total is now 12 phases (6 complete, 6 not started) — `Phases: 6 of 12 complete` in ROADMAP.md's Current Milestone header.

### 09-01 unified (2026-09-08)
`09-01-SUMMARY.md` finalized — all 6 ACs Pass. Loop closed. Not the last plan in Phase 9 (09-02 chat UI remained TBD at the time) — no phase transition triggered then.

### 09-02 applied+unified (2026-09-08): conversational chat UI complete — Phase 9 fully closed
Executed via `/coder:apply` after explicit user approval ("Yes, plan 09-02"). Built `components/assistant-chat.tsx` (SSE client via `fetch`+`ReadableStream`, not `EventSource` since 09-01's `/chat` is POST-only; propose/confirm UI gate) and `components/assistant-launcher.tsx` (floating button, signed-in-only via `<Show when="signed-in">`), mounted globally in `app/layout.tsx` alongside `<Header />`. Two real bugs found and fixed during live browser (Playwright) verification, neither caught by the plan itself: (1) `agent-service/customer-assistant/server.ts` (09-01's own file) had zero CORS headers — direct curl testing during 09-01 never surfaced this since curl doesn't enforce CORS; fixed with a `withCors()` helper + `OPTIONS` preflight, judged an in-scope transport-level fix, not a contract change. (2) A genuine race condition: clicking Confirm the instant the proposal card rendered fired a second `/chat` POST on the same `conversation_id` before the propose turn's SSE stream had closed, corrupting the model's context and causing it to re-propose instead of confirming — isolated via a sequential-turn curl test (proved the model handles identical phrasing correctly with no overlap), then fixed structurally by adding `disabled={sending}` to the Confirm/Cancel buttons. All 4 ACs live-verified against real Mongo/Clerk/Stripe data through the actual running 09-01 process: AC-1 (launcher persists across `/` and `/bookings`), AC-2 (streaming token growth + tool indicators), AC-3 (propose/confirm gate — zero requests fire while the card shows, confirm click only fires once re-enabled, and a direct MongoDB read confirmed the booking's `from`/`to` and `modificationHistory` genuinely updated post-confirm), AC-4 (`memberId` sourced from `useUser().user.id`, never hand-typed). `npx tsc --noEmit` clean throughout. One deviation: mounted in `app/layout.tsx` rather than `components/header.tsx` (plan explicitly allowed either, matching the existing `<Header />` global-mount convention). All temporary verification scripts (`verify-assistant-ui.ts`, `check-booking.ts`, `seed-and-cookie.ts`) deleted per project convention. `09-02-SUMMARY.md` created. **Phase 9 is now 2/2 plans complete** — this is the last plan in the phase, so the transition rule applies: ROADMAP.md's Phase 9 entry should be marked ✅ Complete and Current Position should move to the next not-started phase (Phase 10, Agent Lab & Observability/Evals) on the next planning pass.

### 09-03 planned+applied+unified (2026-09-08): conversational UX upgrade — Phase 9 reopened then reclosed
Per user request ("Look at the travel assistant it is not showing results beautifully... make it very neat"), reopened Phase 9 (per the Phase 6 `06-06` reopen/reclose precedent) for a targeted UX plan: hybrid (structured graph filter + Phase 7 `resolveSynonym` thesaurus) `search_inventory` filtering by vehicleClass, a tightened conversational `SYSTEM_PROMPT` (one short sentence, never restates card-rendered details), and tool-name-keyed rendering of `search_inventory` results as a horizontal strip of the existing `VehicleCard` component in `components/assistant-chat.tsx`. AG-UI protocol evaluated and rejected (full multi-agent interop schema, no functional gain for this single-backend/single-frontend setup); vector-embedding-based discovery evaluated and deferred as a future candidate (no embedding infra exists yet; graph+synonym filter covers the vast majority of real search intents given this project's inventory scale). Live Playwright verification (real Clerk sign-in, real Mongo/graph data) found and fixed 4 issues, 2 during initial live-model testing and 2 during a dedicated visual-polish pass driven by direct screenshot review (not just assertions): (1) model omitting `vehicleClass` from tool calls despite explicit user intent — fixed via strengthened tool-schema/system-prompt wording; (2) verbose markdown prose restating card details — fixed via a tightened one-sentence prompt rule; (3) `VehicleCard` truncated inside the narrow chat strip because its `sm:flex-row` Tailwind breakpoint is viewport-width not container-width — fixed entirely in `assistant-chat.tsx` (widened strip item to `w-[440px]`, `DialogContent` to `sm:max-w-lg`, `MAX_VISIBLE_CARDS` to 3), `components/vehicle-card.tsx` itself left unmodified per the plan's boundary; (4) chat panel not auto-scrolling to the newest message/proposal card — fixed with a `scrollRef`+`useEffect` keyed on the `messages` array. All 4 ACs Pass. `npx tsc --noEmit` clean throughout. All temporary verification artifacts (`tests/e2e/smoke/verify-ui-tmp.spec.ts`, `verify-filter-tmp.ts`, `/tmp/cookie.txt`, `/tmp/chat-raw*.txt`, `/tmp/ui-*.png`) deleted; dev servers stopped. `09-03-SUMMARY.md` created. **Phase 9 is now 3/3 plans complete** — re-transitioned to Phase 10 (Agent Lab & Observability/Evals), still not started.

## Accumulated Context

### Decisions
- Phase 3 (03-01): use latest Next.js (15.x/React 19) and latest `@clerk/nextjs`, not pinned to `mastech-agentic-commerce`'s older Next ^14.2.35/React 18 — user-approved override of an adversarial-review flag. Implication: Next 15's async dynamic APIs (`params`/`searchParams`/`cookies()`/`headers()`) apply here; agentic-commerce snippets referencing those APIs synchronously will need adapting when copied in later phases, not pasted verbatim. Resolved to Next 16.3.3/Tailwind v4 at apply time (that's what "latest" was on 2026-08-28); Tailwind v4 uses `@tailwindcss/postcss` + `@import "tailwindcss"` in globals.css instead of the classic `@tailwind` directives.
- Phase 3 (03-01): `create-next-app@latest` refuses to scaffold into a non-empty directory even with `--yes` — hand-wrote the scaffold files per the plan's documented fallback instead.
- Phase 3 (03-01): AC-2 revised post-checkpoint — bare `clerkMiddleware()` with a matcher does NOT globally enforce sign-in redirect (confirmed live: `/` returned 200, not a redirect). `mastech-agentic-commerce` itself doesn't do global protection either — it gates per-page via `auth()`/`redirect()` (see `app/user-profile/page.tsx`). Since 03-01 is scaffold-only with no protected page yet, AC-2 now covers only that Clerk auth plumbing works (sign-in/sign-up render with real keys), confirmed live. Route-level gating (`auth.protect()` or per-page checks) is deferred to whichever Phase 4+ plan adds the first page that needs it.
- Design system/component library (shadcn or similar, per agentic-commerce's storefront) and a Figma prototype review before design work — raised by user during 03-01 APPLY, explicitly out of scope for this plan (minimal placeholder page only). Added to ROADMAP as a Phase 4 pre-step (see below) — needs its own plan before Phase 4 booking UI work starts.
- New sibling repo (not an evolution of BookCars) — different domain model and stack
- Mirrors `mastech-agentic-commerce`'s Discovery/Checkout vs. Fulfillment separation
- UC1, UC2, UC3, UC6 high priority; UC4, UC5, UC7, UC8 low priority (per stakeholder direction)
- Phase 1 split into 01-01 (Vocabulary/Taxonomy/Thesaurus layer design, extending existing ONTOLOGY.md) and 01-02 (Postgres+AGE store stand-up + real seed load), mirroring agentic-commerce's 5-layer (Vocabulary/Taxonomy/Thesaurus/Ontology/Knowledge Graph) model
- Graph store infra lives under `graph/` at repo root (docker-compose, init SQL, Python loader/verify scripts), port 5434 to avoid colliding with agentic-commerce's 5433 if both run locally
- Query-layer contract for Phase 2/3 to consume the graph is explicitly deferred, not part of either Phase 1 plan

- Phase 4 resequenced (2026-08-28) into 5 vertical-slice plans instead of a "04-00 design + 04-01 TBD" placeholder: 04-00 (design, no deps, ready now) → 04-01 (discovery/search, reads 03-02's graph layer) → 04-02 (checkout+booking, closes UC1, consumes 03-03's Stripe/types, adds the deferred graph-write query + deferred mailHelper) → 04-03 (modification/UC2) and 04-04 (cancellation+refund/UC3), both depending only on 04-02, not each other. Reasoning: each plan needs a real prior output to build against (04-02 needs 04-01's selection UI; 04-03/04-04 need a real Booking from 04-02) — collapsing these would force >3-task plans and vague task actions that get revised once upstream outputs actually exist.
- 2026-08-28: 03-03 APPLY completed live with a real Stripe test-mode key — user chose to reuse the same sandbox/key already used for `mastech-agentic-commerce` rather than provision a second Stripe sandbox. Trade-off accepted: test PaymentIntents/customers from both apps co-mingle in one Stripe dashboard view (cosmetic only, no functional effect). All 5 ACs (AC-1, AC-2, AC-3, AC-4, AC-5) passed on first verification, no deviations.
- 2026-08-28: Both 03-03-PLAN.md and 04-00-PLAN.md passed the standing capped adversarial review. 03-03's punch list (7 items): Booking type field mismatch (`memberId`→`member` to match `lib/models/Booking.ts`'s real ObjectId field), full-tree secrets grep (was staged-diff-only), explicit `{ error: string }` 400 body, new AC-5 for Stripe customer-reuse (no duplicate customers), Vendor-model double-registration verify step, `package-lock.json` added to files_modified, `BookingStatus` reworded from "enum" to "string union." 04-00's punch list (12 items, higher-severity theme): AC-4/boundaries/checkpoint all asserted a false premise that Clerk's matcher excludes `/prototype` (it doesn't — the matcher is a catch-all; `/prototype` stays public only because `clerkMiddleware()` enforces nothing by default and nothing links to it) — reworded throughout; `depends_on: []`→`["03-01"]` plus a genuine Prior-Work reference (plan's own text relies on 03-01's Tailwind v4 decision); added `app/layout.tsx`/`app/page.tsx` to DO NOT CHANGE (no nav exists yet — nothing stopped an agent from creating one to link `/prototype`); mock-data field names corrected to align with `lib/graph/queries.ts`'s real `class_name`/`term_id`/`perk_id` shapes (previously invented non-existent fields); Task 1/2 `<verify>` steps made actually executable by an autonomous agent (added component-existence `ls` check, replaced browser-console check with curl+build); `tailwind.config.ts` added to Task 1 files; pinned `npx shadcn@latest init`; added a concrete AC-4 line to `<verification>`.

### Deferred Issues
- Mongo↔Clerk webhook handler to populate `clerkUserId` — deferred to Phase 4 (no real sign-up flow needs it yet)
- Production Mongo hosting decision — local docker-compose sufficient for now
- Landing page (`app/page.tsx`) doesn't visually match `/prototype/landing` (hero photo, why-us grid, vendor list, browse-by-vehicle-type) — 04-06-PLAN.md now created to close this (real graph data, not mock data), per user decision during 04-05's checkpoint (2026-08-28)
- 04-05's AC-2 negative case (tampered Stripe amount → 400, no Booking) was verified by code review only, not a live tampered request — worth a live test if UC2/UC3 build on the same integrity check
- 2026-09-04: 04-13/04-14's add-on-recompute-on-modify logic (waived-by-new-vendor-perk re-filtering, nights-rescaled fee totals folded into `deltaCents`) was verified by code trace against 04-05's identical booking-creation recompute pattern only — the dev DB currently has zero bookings, so no live reserved-booking-with-add-ons exists to drive a real vehicle-change modify through and diff the response. Closed by Phase 6's planned 06-01 regression suite, not before.

### Data enhancements made during APPLY (user-approved)
- `bookings_1000.json` gained `rental_id` (FK to Inventory, backfilled by provider+city+vehicle exact match, all 1000 matched) and `requested_addons` (~33% of rows) — original file had neither, leaving `FOR_INVENTORY` and `REQUESTED` edges unseedable otherwise. See `01-02-SUMMARY.md` Deviations for detail.
- `graph/init/01-init-graph.sql` filename (not `001-...` as originally planned) — the `apache/age` image's own baked init script doesn't run in guaranteed alphabetical order with mounted files; renamed to sort reliably after it.
- Plan 01-03: expanded vendors 5→10 (Hertz, Thrifty, Dollar, Sixt, Payless added), `Inventory` 500→1000 rows, `NegotiatedTerm` 15→30, `VendorPolicy` 5→10. `rental_inventory_500.json` renamed to `rental_inventory.json`. Added BookCars-aligned fields (`Vendor.minimum_rental_days/price_change_rate`, `Inventory.vehicle_type/gearbox/seats`). Sixt's real `"300 mi/day"` mileage and Payless's real `"Compact Plus"` VehicleClass alias converted the Thesaurus layer from illustrative-only to real seeded `VocabularyTerm`/`SYNONYM_OF` data. See `01-03-SUMMARY.md`.
- Plan 02-01 (new Phase 2, inserted after Phase 1 per user decision — renumbered old Phases 2–5 to 3–6): new `Intent` node type (10 rows, `data/synthetic/agent_intents.json`) + `TARGETS` edge for action/intent-level vocabulary (the confirmed gap in `mastech-agentic-commerce`'s own thesaurus pattern); `synonyms` list property added directly on `VehicleClass`/`Perk`/`AddOn`/`MembershipTier` nodes (attribute-level, ported from that project's pattern). Ontology/KG only — no tool-calling contract yet. See `02-01-SUMMARY.md`.
- Plan 03-02: established a standing, capped adversarial plan-review practice for all future PLANs — max 3 agents, max 2 rounds, run strictly sequentially (never Workflow-tool fan-out), grounded in `~/.claude/coder/references/subagent-criteria.md`. Applied to 03-02 itself: caught real defects (AGE parameterized-cypher binding shape; pooled-connection `search_path` reset) before APPLY.
- Plan 03-02: AC-3's "9 VehicleClass nodes" target was stale by APPLY time — Phase 2's later ontology work added 3 grouping nodes (Standard/Utility/Premium). Actual/correct seeded count is 12, confirmed via direct cypher query, not a code defect.
- Plan 04-04: adversarial review (2 rounds) caught before APPLY: (1) `NegotiatedTerm` is keyed by (Vendor × MembershipTier) not Inventory — a naive `Vendor-[:OFFERS_TERM]->NegotiatedTerm` join would fan out 3 rows/Inventory item; fixed by resolving each vendor's lowest-rank-MembershipTier term as the anonymous-search "starting rate." (2) Task 2's verify command curled the literal route-group path `/(discovery)/search`, which 404s — Next.js strips route-group folder names from the resolved URL; fixed to `curl localhost:3000/search`.
- Plan 04-04 APPLY: `runCypher()` in `lib/graph/client.ts` needed an additive `columns` parameter — AGE requires the SQL `AS (...)` column list to match the Cypher `RETURN` clause's arity exactly, and the existing wrapper was hardcoded to a single `(result agtype)` column. `searchInventory()`'s multi-column `RETURN i, vc, v, l` (and the vendor-lowest-term lookup's `RETURN provider, rank, t, p`) needed explicit column names. Backward-compatible: `columns` defaults to unset, so the 3 pre-existing single-column callers (`getVehicleClasses`, `getNegotiatedTermsForVendor`, `getPerksForTerm`) are unchanged. Not called out in the original 04-04-PLAN.md — a plan-level gap the adversarial review didn't catch (both reviewer rounds assumed the plan's own claim that `runCypher` could be reused "exactly as the existing three functions do," rather than checking AGE's column-arity requirement against a multi-column RETURN).
- Plan 04-04 post-APPLY bug (caught by user manual testing, not by build/curl verification): the sidebar `<form>` in `app/(discovery)/search/page.tsx` had no submit button — filtering logic itself was correct (verified server-side via curl: 1000→100 rows for a location filter, →116/111 for class filters), but there was no way to trigger the vehicle-class `<Select>`'s GET submit in a browser. Fixed by adding a `Search` submit button; rebuilt clean, refiltered live to confirm. Lesson: `npm run build` + curl verify the data layer and route existence, not actual interactive usability — this class of bug (a missing/broken control) won't surface without someone actually clicking through the UI.
- 2026-08-28: 04-04's real `/search` route has no landing-page entry point yet — `app/page.tsx` is still the Phase-3 scaffold placeholder, explicitly boundary-protected in 04-04-PLAN.md (out of scope for that plan). User decision: defer wiring a real landing→search entry point to 04-05, so the full discovery→checkout flow is wired end-to-end at once rather than landing being connected before checkout exists to receive it.

### Git State
Last commit: 52a068f — feat(07-agent-grounding-ontology-enrichment): equivalence clusters, synonym backfill, composable retrievers (Phase 7 complete, 3/3 plans)
Branch: main
Feature branches merged: none
Nothing to push (no push requested/performed).

### Blockers/Concerns
None currently — 04-07 and 04-08 have both been applied and unified (see Loop Position below). Phase 4 is now at 100% except for 04-13, which remains TBD/roadmap-only (needs an availability-check signal not yet in the ontology).

### Adversarial review results — round 1, API-only drafts (2026-09-02, superseded)
- **04-07-PLAN.md** (1 round, 1 reviewer): ownership-check helper misuse and delta-cents rounding fixed. Superseded by the round-2 rewrite below (added UI + vendor-integration boundary module).
- **04-08-PLAN.md** (1 round, 1 reviewer): blocking VendorPolicy field-name defect fixed, `no_show_fee_percent` seed task added, Stripe double-refund guard added. Superseded by the round-2 rewrite below.

### Plan rewrite (2026-09-02): UI + vendor-integration boundary module
Per explicit user request, both plans were fully rewritten to (a) add a real UI on `app/(checkout)/confirmation/[bookingId]/page.tsx` — a "Modify your booking" card for 04-07, a "Cancel booking" dialog for 04-08 — mirroring 04-12's checkout UI conventions (shadcn `Card`/`Button`, Stripe Elements for delta payments), and (b) introduce `lib/vendor-integration/policy.ts`, a new boundary module (per PROJECT.md's Discovery/Checkout-vs-Fulfillment architecture) that isolates vendor-contract-varying business rules — `quoteModification()` (04-07) and `quoteCancellation()` (04-08) — as the seam Phase 7's real, independently-deployed Vendor Integration Layer will later replace with an RPC call, without requiring route/UI changes. Both plans' Task-1-equivalent now branch symmetrically on whether the shared file already exists (append vs. create), so either plan can apply first without clobbering the other's export.

### Adversarial review results — round 2, UI + boundary-module rewrite (2026-09-02)
- **04-07-PLAN.md** (1 full review + 1 targeted spot-check round, 2 reviewer agents total): found and fixed 7 issues — most severe: a `dryRun` flag that wasn't a universal no-op (would silently apply a "preview" as a real modification when the previewed date was cheaper — fixed by making the dry-run check unconditional and first); an orphaned-Stripe-charge risk if the quote drifts between confirming payment and applying (now disclosed as an accepted known gap with a non-misleading error message, not silently unhandled); an addon double-charge risk when a modification changes vendor/inventory and the new perks would waive a previously-charged addon (fixed by re-filtering via `getWaivedAddOnIds()` against the new quote before recomputing); a float-rounding mismatch between `deltaCents` and `/api/payments/intent`'s dollar-based amount (fixed with a ±1 cent tolerance); a missing explicit Booking fetch before the ownership check; an Objective/Output section that overclaimed UI parity with the API's inventory/vendor-change capability (corrected — UI is dates-only); and the file-existence branch for the shared `lib/vendor-integration/policy.ts` module. All 6 spot-checked fixes verified PASS on the second targeted round.
- **04-08-PLAN.md** (1 full review + 1 targeted spot-check round, 2 reviewer agents total): found and fixed 5 issues — most severe (caught only on the spot-check round): the atomic `findOneAndUpdate` cancellation-claim (added to close a concurrent-double-refund race) was followed by a plain `.save()` on the stale pre-claim `booking` object, which would have silently reverted `status` back to `'reserved'` — fixed by requiring the `cancellation` field write to target the already-`'cancelled'` document (a follow-up update or re-fetch), never the stale in-memory object. Also fixed/disclosed: a same-file-clobber risk with 04-07's Task 1 (now symmetric); the dry-run-preview-vs-confirm staleness gap for AC-6 (disclosed as an accepted hours-scale edge case); and `quoteCancellation()`'s handling of a negative `hoursUntilStart` (past-start/no-show) explicitly documented as folding into the standard inside-window rate rather than a distinct rule, since the seed data has no field for a finer distinction.
- Both plans used 2 of the allowed max-3-agents/max-2-rounds budget (1 full review + 1 targeted fix-verification pass each) — within the standing cap. No further review round is planned; both plans are considered adversarially clean pending user read-through.

### Plan addition (2026-09-02): "My Bookings" account page
Per explicit user request ("modifications and cancellations can only be done on the confirmation page which is very restrictive — add a My Bookings page for logged-in users, list all bookings with filters, select and do the necessary actions"), both plans were extended again:
- 04-07 gained Task 4: `GET /api/bookings` (own-bookings listing, `status`/`from`/`to` filters via a new `buildOwnBookingsFilter()` helper exported from `app/api/bookings/route.ts`) + a new real `app/(account)/bookings/page.tsx` list page (status/date filters, per-row "Modify" dialog reusing `BookingModifyForm`) + a "My Bookings" header nav link. New AC-6.
- 04-08 gained Task 6: a per-row "Cancel" entry point on the same `app/(account)/bookings/page.tsx`, reusing `BookingCancelDialog`, symmetric with 04-07's Task 4 on file-existence (whichever plan applies first creates the page/helper; the other appends its entry point). New AC-7.
- Adversarial review (1 agent, 1 round, scoped to just this addition — separate budget from the round-2 review above) found and fixed 2 real issues: (1) 04-07's Task 4 originally lacked the symmetric "page already exists → append, don't recreate" clause that 04-08's Task 6 already had — fixed by adding it explicitly. (2) The `from`/`to` query-param filter semantics were prose-only with no concrete Mongoose query shape, risking the two plans' independently-written filter logic diverging — fixed by pinning the exact filter object and factoring it into a shared, named `buildOwnBookingsFilter()` helper both plans import rather than each re-deriving. Also clarified (non-blocking) that the mock `app/prototype/bookings/page.tsx` status-badge maps are a structural pattern only — the real `BOOKING_STATUS` enum doesn't match its keys. No ownership/security leak or factual API mismatch was found (the GET route correctly scopes to `Booking.find({ member: member._id })`).

## Loop Position

```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [09-07 loop closed]
```

### 09-07 closure (2026-09-15)
- `/agentic-travels` basePath convention applied, unified. SUMMARY: `.coder/phases/09-customer-driver-assistant-agent/09-07-SUMMARY.md`
- 2 deviations found via live manual QA and fixed in the same pass: agent-service `tools.ts`/`env.ts` (cross-process fetch calls into this app's own routes), `components/assistant-payment.tsx` (booking-creation POST after chat payment — root cause of a live "Booking could not be created" failure). Both outside the plan's original `files_modified` scope.
- New tech debt logged: TD-11 in `.coder/ROADMAP.md` (intermittent "no available rental cars" — investigated, no code defect found, deferred to Phase 10 Phoenix tracing).
- **Note:** `09-04-PLAN.md` and `09-05-PLAN.md` in this phase directory have no matching SUMMARY.md — pre-existing gap from before this session, not addressed here; phase-completion transition is NOT triggered by this closure (PLAN count > SUMMARY count in the phase dir either way, since 09-08 is about to be created below).

## Session Continuity

Last session: 2026-09-15
Stopped at: 09-07 loop closed (basePath convention + 2 live-discovered fixes). New plan being created: `09-08` (bookings sort/filter conversational tooling).
Next action: `/coder:plan` for 09-08, then adversarial review + `/coder:apply`.
Resume file: .coder/phases/09-customer-driver-assistant-agent/09-08-PLAN.md

---
*STATE.md — Updated after every significant action*
