---
phase: 06-regression-test-infrastructure
plan: 06
type: execute
autonomous: true
---

<objective>
## Goal
Add unauthenticated smoke coverage for `/bookings` (My Bookings) and correct the now-stale "no Clerk test-auth fixture" comment in `tests/e2e/smoke/smoke.spec.ts`.
</objective>

<context>
@.coder/PROJECT.md
@tests/e2e/smoke/smoke.spec.ts
@app/(account)/bookings/page.tsx
</context>

<acceptance_criteria>
## AC-1: Unauthenticated smoke coverage exists for /bookings
Given an unauthenticated Playwright context
When the smoke suite navigates to `/bookings`
Then the response status is below 500 and the page renders the "Sign in to see your bookings" sign-in gate, matching every other route group's reachability/gate pattern already in the suite

## AC-2: Stale fixture comment corrected
Given `06-01` added a real Clerk `@clerk/testing` auth fixture (`tests/e2e/regression/`, `auth.setup.ts`) to this repo
When a reader reaches the in-code comment above the checkout-gate tests in `smoke.spec.ts`
Then the comment no longer claims "No Clerk test-auth fixture exists in this repo" and instead states that a fixture exists in the `regression` project but smoke deliberately stays unauthenticated-only
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Add /bookings smoke test and fix stale comment</name>
  <files>tests/e2e/smoke/smoke.spec.ts</files>
  <action>
    Add a new test inside a `test.describe('account routes', ...)` block (new describe, placed after the existing `checkout and confirmation routes` describe):
    - `test('/bookings shows the sign-in gate for an unauthenticated user', ...)`: `page.goto('/bookings')`, assert `response?.status()` is below 500, then assert `page.getByText('Sign in to see your bookings')` is visible — mirrors the existing `/checkout` sign-in-gate test's structure (lines 73-77).
    Then edit the comment at lines 68-72 (above `/checkout with a valid inventoryId/vendorId still reaches the sign-in gate`): replace the claim "No Clerk test-auth fixture exists in this repo" with an accurate statement that `06-01` added a real fixture (`tests/e2e/regression/auth.setup.ts`, `@clerk/testing`) used by the `regression` Playwright project, and that this smoke suite still avoids driving signed-in flows on purpose, per this phase's Scope (unauthenticated reachability/rendering only) — so these two checks still only prove the page doesn't throw before hitting the auth gate.
    Avoid: don't add a `regression` project reference or import into this file — smoke stays in the `smoke` project, unauthenticated only.
  </action>
  <verify>npx playwright test --project=smoke tests/e2e/smoke/smoke.spec.ts</verify>
  <done>AC-1 and AC-2 satisfied: new test passes, comment no longer stale</done>
</task>
</tasks>

<output>
After completion, create `.coder/phases/06-regression-test-infrastructure/06-06-SUMMARY.md`
</output>
